# v0.17.0 kickoff — audit, perfect, and complete ALL consolidators + comparators

> **Start this in PLANNING mode.** It is a long road that will need **compacting**
> along the way — keep this file + [tsn-parsers.md](tsn-parsers.md) +
> [roadmap.md](roadmap.md) as the durable plan so a compact never loses the thread.
> **Spare no expense:** use subagents/workflows for the audit + per-report builds,
> and verify every count **≥3 independent ways**. **Update the relevant `.md` files
> as you go** — the doc-update checklist below is part of "done," not an afterthought.

---

## ▶ PROGRESS — resume here (branch `v0.17.0-audit-complete`)

Work is on the branch **`v0.17.0-audit-complete`** (off `main`). Full blocking
golden-check suite is **green** at each commit below. Raw ground truth: the local-only
`C:\Users\Yunus\Downloads\TSMIS\All Reports 6.19\` (TSN + TSMIS for all six reports).

**Done (committed):**
- **Phase 0** (`98467f6`) — canonical TSN library `scripts/tsn_library.py` (+ `paths.TSN_LIBRARY_ROOT`);
  `matrix.tsn_source`→library; `day_matrix.TSN_SUBDIR` deleted → per-row `tsn_subdir`. HL canary unchanged.
- **Phase 1** (`98467f6`) — HL-PDF silent-row-drop trio (`consolidate_tsmis_highway_log_pdf.parse_pdf`
  returns reconciliation stats; ⚠ INCOMPLETE / carried-forward banners) + `check_tsmis_pdf_reconcile.py`.
- **Phase 2** (`dd70f10`) — **reference FLAT recipe**: `compare_ramp_detail_tsn.py` + `tsn_load_ramp_detail.py`;
  `compare_core` opt-in `context_fields` + `extra_sheet_writer` (no-op defaults; **Route-1=969 re-verified**).
  Canary locked in [tsn-parsers.md](tsn-parsers.md): 15,211 both / 902 diff / **0 context diffs**.
- **Phase 2b** (`5039a51`) — matrix vs-TSN dispatch generalized: `matrix.tsn_comparator_for(row_key)` +
  `tsn_supported(row_key)` (one registry → flips a report on in BOTH matrices); `consolidate_and_compare_tsn`
  keyed on `(row_key, subdir)`; consolidation registry-driven. Ramp Detail now live in both matrices.
- **Phase 3 (started)** (`adb6928`) — `consolidate_intersection_detail.py` (thin `consolidate_xlsx`
  wrapper; 218 routes → 16,473 rows) + `check_consolidate_intersection.py`.
- **Phase 3a — Ramp Summary vs TSN (AGGREGATE) + `summary_layout.py`** — the 2nd recipe.
  `consolidate_ramp_summary` completed to the full **16 ramp types** (added TSN-only **P/V** "Dummy"
  classes; `clean_label` extended to strip the TSN page's bracket/lowercase-total noise — TSMIS page
  unchanged, re-verified). New `summary_layout.py` (canonical category spec + familiar-layout
  `extra_sheet_writer`), `compare_ramp_summary_tsn.py` (sums consolidated TSMIS vs statewide TSN PDF;
  key=category, `has_route=False`), `tsn_load_ramp_summary.build_into` (+ registered in `tsn_library`),
  `check_compare_ramp_summary_tsn.py`. Wired: COMPARE_REPORTS + `matrix.tsn_comparator_for("ramp_summary")`
  + app.spec + checks.yml. Also added a **blocking product-name guard** (`check_no_misspelling.py`).
  **Canary locked** (tsn-parsers.md): 31 both / 1 only-TSMIS / 27 diff / 4 identical; TSMIS 15215 vs TSN 15410.
- **Phase 3b/3c — Intersection Summary (consolidator + AGGREGATE vs-TSN)** — `consolidate_intersection_summary`
  (block-walk category summer; 218 routes → 16,473) + `compare_intersection_summary_tsn` +
  `tsn_load_intersection_summary` + `summary_layout.INTERSECTION_SUMMARY_SPEC` (11-block union, 72 categories)
  + the shared spec-driven `counts_from_rows` mapper (used by BOTH sides). TSN side = a 3-COLUMN PDF (band-split
  + block-walk). Diverged CONTROL/INTERSECTION-TYPE codes show **one-sided** via `Cat.sides` + `categories_for(side)`
  (user decision). Fully wired + golden checks. **Canary:** 72 union / 56 both / 10 only-TSMIS / 6 only-TSN / 52 diff; 16473 vs 16626.

**Recipes proven (reuse these):**
- **FLAT vs-TSN** = `compare_ramp_detail_tsn` — per-side loaders project onto one PM-keyed shared
  header; side-only columns → non-counting `context_fields`. Use for Intersection Detail + Highway Sequence.
- **AGGREGATE vs-TSN** = `compare_ramp_summary_tsn` (BUILT) — each side → one statewide `{category:count}`
  table; compare `has_route=False`, `header=[Category, Count]`, `key_field=0`. Familiar "Summary by
  Category" sheet via `summary_layout.make_extra_sheet_writer(SPEC)`. TSN-only categories (P/V) → TSMIS
  0 (both-sided diff); a TSMIS-only metric → emitted on the TSMIS side only (footnote). Use for Intersection Summary.
- Enabling a report in BOTH matrices = add it to `matrix.tsn_comparator_for` (+ its consolidator to
  `reports._CONSOLIDATOR_BY_SUBDIR` if FLAT).

- **Phase 3d — Intersection Detail vs TSN (FLAT)** — `compare_intersection_detail_tsn` +
  `tsn_load_intersection_detail` + golden check. Read TSMIS **by position** (the planning "pair-order
  reversal" was a shifted-header misread — both sides are (eff_date, type)); **`Y↔1 / N↔0` boolean
  normalize** (user decision) with a Notes indicator sheet; control-type taxonomy = genuine diffs;
  cross-street attrs (TSMIS blank ~37%) + Date of Record + PR = context. Canary: 16180 both / 293
  only-TSMIS / 446 only-TSN / 5520 counted diff (0 context); 16473 vs 16626. **Intersection is COMPLETE.**
- **Phase 3e — Highway Sequence vs TSN (FLAT, route+county+PM)** — the **LAST report; comparator goal COMPLETE.**
  New `consolidate_tsn_highway_sequence.py` (word-level parse of the 12 district `Highway Locations` PDFs —
  NOT char-window; columns are widely spaced, only the 2-char G/RF flag splits into HG+FT; equate annotation
  lines emitted to pair with TSMIS `END R REALIGNMENT`) + `compare_highway_sequence_tsn.py`. KEY = route +
  **COUNTY** + PM (CA postmiles are county-relative — the planning "route+PM" was wrong; `key_normalizer`
  composites `"COUNTY POSTMILE"`, County stays its own column). TSMIS read **by position**, prefix+PM+suffix
  re-glued. **County trailing-period strip** (`LA.`→`LA`) was essential — without it whole counties went
  one-sided. FT + Description compared; HG/City/Distance = context (completeness gaps + listing-granularity
  artifact) with a Notes indicator. Fully wired (tsn_library, matrix.tsn_comparator_for, COMPARE_REPORTS,
  app.spec, checks.yml) + golden `check_compare_highway_sequence_tsn.py`; the stale matrix-check assertions
  (highway_sequence was the "greyed example") updated. **Canary:** 57,070 both / 3,369 only-TSMIS / 12,688
  only-TSN / 5,538 diff (FT 699 + Description 4,839; 0 context); 60,439 vs 69,758 rows; 242 routes both.
  **ALL 6 reports + HL-PDF now compare vs TSN in both matrices.**

**Phase 4 UX (in progress):**
- ✅ **4a — Settings ▸ TSN reports status panel (DONE).** `gui_api`: `get_settings().tsn_library`
  + `tsn_library_status()` / `import_tsn_raw(report)` (native dialog → `tsn_library.import_raw`) /
  `rebuild_tsn_library(report)` (→ `tsn_library.build_consolidated(force=True)` on the shared slot via
  `ConsolidateWorker`). UI: `#setTsnLibrary` + `renderTsnLibrary` (green/amber/grey dot, status line,
  Import/Rebuild); auto-refresh via `S._tsnRebuildPending` in the `"state"` handler. Mock + the 3 stubs;
  golden `check_gui_bridge.test_tsn_library_panel`. **Verified in `#mock`** (render, Rebuild busy→current,
  Import none→has-raw; no console errors). Docs: gui.md.

- ✅ **4b — Drag-drop row/column reorder (DONE).** `settings.{get,set}_{matrix_row,matrix_env,
  day_matrix_row}_order` (order-preserving) + `matrix.apply_order(keys, order)` (sort-key over the
  visible rows/cols; preference not filter) applied in `matrix_snapshot` (row+env) and
  `day_matrix_snapshot` (row). Bridge: `set_matrix_row_order` / `set_matrix_env_order` /
  `set_day_matrix_row_order`. UI: `dndAttach` (one HTML5-DnD helper; grip + `--primary` drop edge)
  on both matrices' row + column headers. Mock updated (order applied + persisted; stale
  tsn_capable/supported flags fixed to all-true). Golden `check_matrix.test_reorder` +
  `check_matrix_bridge`. **Verified in `#mock`** (synthetic drag on both matrices, rows + env
  columns, persistence). Docs: gui.md.

**Remaining (next session):**
1. **Phase 4c** — per-cell/row consolidate buttons on the vs-TSN matrix.
2. **Phase 4d** — the add-day → export → consolidate → compare pipeline.

**Guardrails to keep (every session):** core console-free; raw data LOCAL ONLY (never commit/copy/push);
`compare_core` regression-locked (opt-in `CompareSchema` fields only; HL Route-1 = 299 both / 969 diff
unchanged); new module → `app.spec`; new check → `checks.yml`; **the product name is ALWAYS TSMIS**
(`build/check_no_misspelling.py` is a BLOCKING guard — it will fail the build on the transposition, so
run it before committing); commit per phase with the canary in the message.

Per-report schema facts + approved counts: [tsn-parsers.md](tsn-parsers.md). Engine opt-in fields +
families: [comparison-engine.md](comparison-engine.md) §3/§9c. Golden checks: [verification-and-testing.md](verification-and-testing.md).

---

## Mission

We now have, in one place, the **raw TSN version AND the raw TSMIS version of all six
reports + the PDF Highway Log** — i.e. complete ground truth. Phase one is therefore
not just "build the missing consolidators/comparators": it is a **full audit, rethink,
and perfection of EVERY consolidator and comparator — existing and new** — so that the
whole report-processing surface is provably flawless. Outcome: all 6 reports + the PDF
Highway Log can **consolidate**, **compare cross-environment**, and **compare vs TSN**,
each proven cell-for-cell, with the docs and golden checks brought fully current.

## How to run it (the standard for this effort)

- **Inspect the raw files FIRST.** Before writing any loader, open the TSN and TSMIS
  file for the report, reconcile them by hand, and record the schema in
  [tsn-parsers.md](tsn-parsers.md). Schema comes from the data, never a guess.
- **The flawless-audit recipe is the bar** — the 8-step per-report recipe in
  [verification-and-testing.md](verification-and-testing.md) ("Per-report flawless-audit
  recipe"). Each consolidator/comparator (existing too) passes it before it's "done."
- **`compare_core` is regression-locked.** Never fork it; add behavior via an **opt-in
  `CompareSchema` field that defaults to the no-op original**. Any formula/label-text
  change needs the `%TEMP%\tsmis_regress` before/after harness and must keep the
  **Route-1 = 299 both / 969 diff-cells** Highway Log canary byte-identical.
- **Console-free core; raw data LOCAL ONLY** (`C:\Users\Yunus\Downloads\TSMIS\…` — never
  commit/copy/push). New modules MUST be added to `build/app.spec` `APP_MODULES` or they
  silently drop from the bundle, and each new `check_*.py` MUST be wired into the blocking
  loop in `.github/workflows/checks.yml`.

## Data manifest (what you're providing)

Everything is in **`C:\Users\Yunus\Downloads\TSMIS\All Reports 6.19\`** (LOCAL ONLY — never
commit/copy/push), raw as exported:
- the **TSN** version of all six reports — a **MIX of Excel and PDF** (each report's TSN
  format is its own; the PDF ones need a parser/consolidator to an Excel for computation), and
- the **most recent TSMIS** exports for the reports.

(Existing HL real pairs also live under `…\TSMIS\inputs\`; HL-PDF per-route PDFs are in the
repo's git-ignored `input/tsmis_highway_log_pdf/`.)

## What we HAVE (reference recipes — audit these too)

- **Consolidators:** `consolidate_xlsx_base` (shared streaming core) + thin wrappers for
  Ramp Detail, Highway Sequence, Highway Log (Excel); `consolidate_ramp_summary`
  (standalone **PDF** parse — the reference for non-XLSX); `consolidate_tsmis_highway_log_pdf`
  (cell-rect PDF) and `consolidate_tsn_highway_log` (char-window PDF — the reference for a
  TSN-side parser).
- **Comparators:** `compare_core` (~1,900-line streaming engine, parameterized by
  `CompareSchema`, **never forked**); `compare_env` (cross-env folders: RS/RD/HSL/HL);
  `compare_highway_log` (TSMIS Excel vs TSN); `compare_highway_log_pdf` (PDF flavors).
- **Shell (shipped v0.16.1):** Compare sub-tabs Cross-environment / vs TSN / vs TSN Matrix;
  the vs-TSN matrix lists every report (HL wired, rest greyed) with plug-in points marked.

## What's MISSING (build)

1. **Intersection consolidators** — `consolidate_intersection_summary.py` +
   `consolidate_intersection_detail.py` (TSMIS side; neither exists). Confirm the export
   **sheet name** from the real file. Intersection Detail likely has a free-text
   **Description** → formula-injection guard. Add to `_CONSOLIDATOR_BY_SUBDIR` +
   `CONSOLIDATE_REPORTS`.
2. **TSN-side consolidators/parsers** for Ramp Summary, Ramp Detail, Highway Sequence,
   Intersection Summary, Intersection Detail — **format UNKNOWN for all five** (PDF? XLSX?
   per-route? per-district?); decide loader strategy per report from the raw file. New drop
   folder per report (`input/tsn_<subdir>/` like `input/tsn_highway_log/`, or a file pick).
3. **vs-TSN comparators** — `compare_<report>` for those five, each a loader +
   `CompareSchema` + `run_compare` call + a `COMPARE_REPORTS` row `("<report> — TSMIS vs TSN",
   module, "files", "tsn")`. Flip `supported` in `day_matrix._day_rows()` + dispatch in
   `build_day_cell`.
4. **`TSN_SUBDIR` generalization (do early, structural):** `day_matrix.TSN_SUBDIR` (and the
   `matrix.consolidate_and_compare_tsn` / `tsn_source` / `tsn_input_root` calls) are hardcoded
   to `"highway_log"`. Generalize to a **per-row `tsn_subdir`** so each report's TSN dataset
   drops into its own `_tsn_input/<subdir>/`. This is the last Highway-Log-specific bit.
5. **Optional cross-env** for Intersection + HL-PDF — needs an `EnvCompare` adapter (and, for
   Intersection, the new consolidator) before it can join the Everything matrix's env mode.
6. **Golden checks** — one per new module (5 vs-TSN comparators, 2 Intersection consolidators,
   plus any `check_tsn_<report>_parse.py` for new PDF parsers) + CI wiring. None exist yet.

## Canonical TSN library + UX features (also in scope — design report-agnostic)

These ride along with the report builds; design each so every report benefits at once.

- **A canonical TSN library — ONE fixed home the whole app references.** The six TSN reports
  essentially **never change**, so stop scattering them across drop folders. Give them one
  managed location (e.g. `<data_root>/tsn_library/<report>/`) holding the **raw** TSN file(s)
  AND the **generated consolidated/Excel** version (built once from the PDF/Excel raw and
  reused for every computation — a couple of the TSN reports are PDFs). Every
  consolidator/comparator/matrix **defaults to this library** (the per-row `tsn_subdir` points
  into it); an explicit **user file-pick stays as an override**. Imports the raw files from the
  data drop above. This supersedes the scattered `input/tsn_highway_log/` + `_tsn_input/…` split.
- **Settings ▸ "TSN reports" status panel.** Per report, show: raw TSN present? consolidated/
  Excel version generated? current (vs the raw)? — with a re-import / re-consolidate action.
  Since these files rarely change, this is the single place to see and refresh them.
- **Reorder rows AND columns on BOTH matrices.** Let the user move report rows and day (or
  environment) columns into a specific order (e.g. days in a chosen sequence) **without**
  resorting to hide/show. Persist the order per matrix, alongside the existing hidden-rows/days
  settings.
- **Per-cell + per-row consolidate buttons on the vs-TSN matrix** (carried from 0.16.x).
- **One-stop pipeline on the vs-TSN matrix** — "add the current day" → export (live) →
  consolidate → compare, chained via the queue; build it report-agnostic (carried from 0.16.x).

## Rethink / perfection candidates (the audit's "fix while we're here" list)

- **TSMIS Highway-Log PDF consolidator — silent row-drop trio (roadmap P2):**
  stale-geometry carry-forward silent corruption, page-skip unlogged when no prior geometry,
  and no row-count verification. **Fix during this pass** (log + guard + runtime count) — it
  affects every PDF-based comparison the matrix shows.
- **`consolidate_xlsx_base` has no distinct `partial` status** — a skipped-file combine still
  returns `"ok"` with only an INCOMPLETE banner in `summary_lines`; downstream can't tell
  programmatically. Decide whether to add a real status (needs consumer support) or keep +
  document.
- **Ramp Summary combined-sheet uses hard-coded cell coordinates** — fragile if the schema
  gains a row; consider deriving them.
- **Unnamed-column `(col X)` labeling lives in `compare_env._load_xlsx_side`, not the
  consolidator** — any TSN Highway-Sequence consolidator must replicate/align it.
- **Route-extraction regex `_route_(\w+)\.xlsx$` is end-anchored** and only works because the
  export-everything prefix is front-stamped — an invisible coupling; document or harden.
- **`consolidate_xlsx_base` is hardwired to one sheet / prepend-Route** — extend carefully (or
  add a parallel entry point) for any TSN layout that differs from TSMIS's.

## Per-report open questions (answer from the raw files before coding)

For **each** of Ramp Summary, Ramp Detail, Highway Sequence, Intersection Summary,
Intersection Detail: TSN file format (PDF/XLSX)? per-route or per-district? the natural
comparison **key** (route / PM / county / composite)? does the TSN schema match TSMIS's?
sheet name (Intersection)? any ditto/roadbed analog? Record answers in tsn-parsers.md.

## Per-report build recipe (the loop)

1. Inspect raw TSN + TSMIS → reconcile by hand → record schema in **tsn-parsers.md**.
2. TSMIS consolidator (Intersection: build; others: audit the existing one).
3. TSN-side consolidator/parser (new drop folder; mirror the HL TSN parser if PDF).
4. `compare_<report>` over `compare_core` with the right `CompareSchema`.
5. Wire: `COMPARE_REPORTS` (group `tsn`), `_CONSOLIDATOR_BY_SUBDIR`, `day_matrix` supported +
   dispatch, per-row `tsn_subdir`, `app.spec` `APP_MODULES`.
6. Golden check(s) + `checks.yml`.
7. **Flawless audit** (the 8-step recipe) → lock approved counts in tsn-parsers.md.
8. Update docs (checklist below).

## Suggested build order

0. **TSN_SUBDIR generalization** + **the HL-PDF P2 fixes** + a re-audit of the existing
   HL/RS/RD/HSL consolidators+comparators against the fresh raw files (they're the reference;
   perfect them first).
1. **One XLSX report end-to-end** (Ramp Detail or Highway Sequence) to lock the per-report
   recipe + the golden-check template.
2. Fan out the remaining XLSX reports.
3. **Intersection** (needs the two new TSMIS consolidators first).
4. **Optional:** Intersection + HL-PDF cross-env adapters.

## Doc-update mandate (per report, as you ship)

- **tsn-parsers.md** — the report's TSN format, column→TSMIS mapping, key, normalization,
  approved counts.
- **comparison-engine.md** — new §9d–§9h family sections; the §12b vs-TSN-matrix per-row
  `tsn_subdir` table.
- **reports.md** — `COMPARE_REPORTS` + `CONSOLIDATE_REPORTS` tables; the add-a-consolidator /
  add-a-comparison recipes (incl. the `app.spec` + `checks.yml` steps).
- **architecture.md** — the `COMPARE_REPORTS` / `CONSOLIDATE_REPORTS` tables + the **canonical
  TSN library** location/model (and how `tsn_subdir` resolves into it).
- **gui.md** — the matrix **row/column reorder** interaction + the **Settings ▸ TSN reports**
  status panel.
- **it-and-security.md / verification-and-testing.md** — where the canonical TSN library lives
  on disk (data-root, not committed) and the per-report input pairs.
- **verification-and-testing.md** — the golden-check catalog (one row per new check) + a
  per-report verification-loop entry (approved counts + local input paths).
- **CLAUDE.md** — the Supported-reports table (Intersection gains consolidate/compare).
- **roadmap.md** — check the Intersection backlog item off incrementally; keep the version
  table current.
- **CHANGELOG.md** + **version.py** at release.

## Guardrails (unchanged)

- `compare_core` regression-locked — opt-in `CompareSchema`, prove cell-for-cell, Route-1=969.
- Core console-free; raw TSN/TSMIS data LOCAL ONLY.
- The 8-step per-report audit recipe is the bar; a module isn't done until its golden check
  is in `checks.yml` and green, and its doc rows are updated.
- The v0.16.1 sub-tab restructure + matrix generalization are **done** — build on them.

**First step (planning mode):** inspect each raw file, answer the per-report open questions,
fill the tsn-parsers.md format rows, and lay out the concrete task graph (TSN_SUBDIR
generalization + P2 fixes + the one-report reference build first). Ask me anything the raw
files don't make obvious.
