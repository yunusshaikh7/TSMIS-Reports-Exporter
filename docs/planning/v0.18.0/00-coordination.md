# v0.18.0 Planning — Coordination

**Repository:** `TSMIS-Reports-Exporter`
**Release target:** **v0.18.0** — structural optimization + engineering-overhaul (maintainability,
architecture, reliability, testing, security, performance, packaging, shipped-app cleanup). Few/no
major new features.
**Evidence baseline:** HEAD `d2ee353`, tag `v0.17.1`, clean tree. Compile green; 44/44 CI golden
checks pass.

## Current planning status

> 🚀 **RELEASED (2026-06-26) — v0.18.0 published to GitHub Releases FROM THE BRANCH (user-approved); `main` untouched.**
> Branch `refactor/v0.18.0-structural-overhaul` pushed; tag **`v0.18.0` @ `a80c100`** pushed → `release.yml` run
> `28268539340` = **success**, 6 assets live (win64 63 MB / win64-with-browser 241 MB / batch-source 2 MB, each
> `+.sha256`): <https://github.com/yunusshaikh7/TSMIS-Reports-Exporter/releases/tag/v0.18.0>. The branch-push `checks`
> run (`28268279281`) passed green on a clean CI runner. **`main` deliberately left at v0.17.8** — the histories
> diverged at `d2ee353` (origin/main got the original v0.17.2–v0.17.8 commits; the refactor **forward-ported** them,
> so `--ff-only` was impossible and a blind merge was declined). **main reconciliation is deferred** (do the
> `-s ours` supersede or a force-update at v0.18.1 GA, once work-PC acceptance passes). **Owed → v0.18.1:** the
> work-PC field acceptance via the evidence kit (`docs/work-pc-validation.md` §3).
>
> ---
>
> ✅ **STATUS: `complete` — v0.18.0 offline candidate RELEASE-PREPARED. Final commit `a80c100` ("prepare v0.18.0").**
> Codex final review = **`READY FOR RELEASE PREPARATION`** (no blockers; approved the close-out — see
> [`phases/final-codex-review.md`](phases/final-codex-review.md)). **`a80c100` is an empty release-anchor commit**:
> per Codex §7 there were **no uncommitted product close-out changes**, because the release-prep product content
> (`version.py` → 0.18.0 + the `## v0.18.0` CHANGELOG + the canonical-doc reconciliation) was already committed in
> **P11 `375b48c`**. **Not pushed; no tag.** The integration & completion pass was verification + release prep only —
> **no product code changed** (no new architectural/behavioral/security/persistence/packaging design; nothing
> substantive discovered). **Full suite:** byte-compile OK · **74/75 Python** (the 1 = `check_no_misspelling` on the untracked
> `docs/planning/…/P10-codex-review.md` hit; CI-clean on the committed tree) · **3/3 Node**. **Frozen gate:** BOTH
> variants PASS — `build.ps1 -SelfTest` (win64, 150 MB) **and** `-SelfTest -BundleChromium` (with-browser, 524 MB),
> each "SMOKE OK" reporting 8 reports / 252 routes on the EXACT shipped exe; `check_source_zip_smoke` green.
> **compare_core 0-diff vs the branch point `d2ee353`** (regression lock held; `context_fill` not ported — 0 hits).
> **Compat reconciliation:** `_safe_join`/`full_snapshot`/`context_fill`/`day_matrix.TSN_SUBDIR` all GONE; the
> permanent `tsn_load_*` + `common.py` shims kept by design. **Branch diff:** 148 files, +22,946/−8,589, 25 commits;
> no stray/secret/artifact paths; `docs/planning/` untracked (not staged). **Uncommitted close-out PRODUCT changes:
> NONE** (version/CHANGELOG/docs were committed in P11 `375b48c`). Report:
> [`phases/final-claude-report.md`](phases/final-claude-report.md). **Owed → v0.18.1:** the work-PC field acceptance
> (`docs/work-pc-validation.md` §3) + push branch/tag — both await the user. **Next: Codex final review of the close-out.**
>
> ---
>
> ✅ **P11 COMMITTED (`375b48c`, the LAST phase) — Codex `PASS` (round 2); not pushed. v0.18.0 candidate code-complete.**
> Docs + audit/roadmap reconciliation (baseline `5d149ea`).
> Reconciled 15 docs + `version.py` (`0.17.1 → 0.18.0`) to the refactored v0.18.0 HEAD: the canonical `docs/`
> (architecture/gui/internals·gui-bridge·export-engine·updater-swap/comparison-engine/engine-and-reliability/
> build-and-release/roadmap) + `CLAUDE.md` (report 6b + the new outcome/transaction/column-layout/catalog
> conventions + repo layout) + `CHANGELOG.md` (v0.18.0 section) + the CR-002 doc set (README, `reports.md`,
> `tsn-parsers.md`). Wrote every **§J2 audit disposition** + the **M03 marker (now implemented)** + the
> **hard-deferrals** into `docs/roadmap.md`; linked `docs/work-pc-validation.md` into `docs/INDEX.md`; preserved the
> two-tier framing. **Discovered + recorded honestly:** `wait-js-fstring-interpolation-unvalidated` is NOT closed in
> code at HEAD (the §J2 "Resolved in P8b" claim is inaccurate) → carried forward to v0.18.1 (NOT implemented in P11).
> Every behavioral claim verified against shipped code first (Report View wired, `CONTEXT_FIELDS=()`, 66-cat Int
> Summary fold, fail-closed checksum, frozen-only cache clear, `common.py` shim, windowed self-test). **Verification:**
> full offline suite **74/75 Python + 3/3 Node** (the 1 = the known untracked-planning misspelling hit; CI-clean on the
> committed tree); tracked content transposition-clean; all relative doc links resolve; diff = **15 docs + `version.py`
> only, NO `scripts/` changes** (`compare_core`/engine/UI untouched). Report:
> [`phases/P11-claude-report.md`](phases/P11-claude-report.md); review: [`phases/P11-codex-review.md`](phases/P11-codex-review.md).
> **Codex round 1 = `BLOCKED`** (P11-B01 stale 7-report/PDF-edition matrix language; P11-B02 stale 1.5 s death-check +
> dev cache-clear in `updater-swap.md`) → **both FIXED** (docs-only; comparison-engine §9c/§12/§12b, reports.md
> `group=env`, architecture B2 + `tsn_subdir`, updater-swap death-check + `cleanup_leftovers` order); P11-N01
> (product-code comment drift) **DEFERRED** (Codex-agreed — out of docs-only scope). Re-verified: `check_report_catalog`
> PASS, residual-drift sweep clean, registry/matrix probe matches (8/8), diff still 16 files (+564/−197), no `scripts/`
> changes. Remediation appended to the report. **Committed `375b48c`** (16 files = 15 docs + `version.py`, +564/−197;
> `docs/planning/` not staged; not pushed). **P11 is the LAST phase — no next pending phase.** The **amended v0.18.0
> offline blocking set is now fully committed** (P0/PA/P1/P2/P3/P4/P5b/P6/P7a/P7b/P7c/P8a/P8b/P8c/P9/P9b/P10/P12/P14/P15/
> P13/P11). **Owed → v0.18.1:** the work-PC field acceptance (the evidence-kit run + the live export/consolidate/compare/
> self-update verification — see `docs/work-pc-validation.md` §3); and pushing the branch + the `v0.18.0` tag.
>
> ---
>
> ▶️ **P13 COMMITTED (`5d149ea`, FINAL design phase) — Codex `PASS` (round 2); not pushed. Next eligible: P11
> (LAST).** The work-PC validation handoff: a **credential-safe, no-admin `--collect-evidence` mode** (new
> `scripts/evidence.py` + the `gui_main` entry, reusing the self-test plumbing), a **manual fallback**, the **§K2
> acceptance checklist**, and the **v0.18.1 close-out plan** — all in `docs/work-pc-validation.md`, over the final
> **8-report** shape (CR002-RM5; the live-verify set is registry-derived). `--evidence-dir` is a **positive
> allowlist** (PDF/XLSX only) + a browser/profile-basename refusal, so a copied cookie store / login DB / `.html`
> page can't leak (P13-B01). **RM05 credential-exclusion proven** by `build/check_evidence_bundle.py` (**35
> assertions** incl. adversarial copied-browser/profile/HTML fixtures + the manifest-from-written test). **Purely
> additive** — `compare_core`/`version.py`/`gui_api` 0-diff; 6 files (+762). Report:
> [`phases/P13-claude-report.md`](phases/P13-claude-report.md); review: [`phases/P13-codex-review.md`](phases/P13-codex-review.md)
> (round 1 `BLOCKED` → P13-B01/R01/A01 fixed → round 2 `PASS`). **The *actual* work-PC acceptance is v0.18.1** (the
> collector is offline-proven; the live run is the field gate). **Resume point: P11** — the LAST phase (docs +
> audit/roadmap reconciliation; absorbs the CR-002 doc set + links `docs/work-pc-validation.md`).
>
> ---
>
> ▶️ **P15 COMMITTED (`c32941e`, CR-002 forward-port) — Codex `PASS` (round 2); not pushed.**
> The Intersection Detail vs-TSN comparison evolution is forward-ported to its **final v0.17.8 state** (position-
> aligned dates, the J–P+S→`S` crosswalk, `CONTEXT_FIELDS=()` compare-everything, numeric-padding norm, read-time
> TSN-library re-normalization, the "Report View" replica via the existing `extra_sheet_writer`) + the §9b Summary
> signal fold. **`compare_core.context_fill` was NOT ported (RM3)** — `compare_core` is byte-for-byte unmodified
> (`git diff` = 0; `git grep context_fill` = 0); `version.py` untouched (RM6). Report:
> [`phases/P15-claude-report.md`](phases/P15-claude-report.md); review:
> [`phases/P15-codex-review.md`](phases/P15-codex-review.md) (round 1 `PASS WITH FIXES` → P15-R01 wording fixed →
> round 2 `PASS`). Both v0.17.8 canaries green (Detail; Summary 66 cats 58·8·0); the Route-1 HL canary + every
> other vs-TSN comparator + `check_compare_audit` unaffected; the PDF comparators inherit the schema with no edit.
> 7 files (+912/−146; 3 product + 4 checks). Statewide canaries (Excel 163,353 / PDF 163,361) are real-data →
> **v0.18.1** (RM04). **Resume point: P13** (work-PC validation handoff + v0.18.1 close-out — the FINAL design
> phase, now over the 8-report shape).
>
> ---
>
> ▶️ **P14 COMMITTED (`380b7e8`, CR-002 forward-port) — Codex `PASS`; not pushed.** The
> Intersection Detail (PDF) report family is registered + wired at full HL-PDF parity in the refactored tree:
> 4 new modules + the 36-col PDF parser, catalog/registry/matrix/day-matrix/compare-env/exporter/reset wiring,
> the `#mock` fixtures (in `mock.js`), `app.spec` + the consolidate `.bat`, the new golden check + fake-site
> fixture, and the append-only `_V017_EXPORT_ORDER` index-7 manifest gate. **`reports.py`/`compare_core`/
> `version.py`/`app.js` untouched** (RM1/RM3/RM6/RM2). Report: [`phases/P14-claude-report.md`](phases/P14-claude-report.md);
> review: [`phases/P14-codex-review.md`](phases/P14-codex-review.md) (`PASS`, non-blocking wording recs only —
> Codex independently ran a browser `#mock` render and confirmed the new rows render). One regression
> (`check_compare_env_highway_log_pdf` last-row → membership, the predicted `b70a644` fix) was found + fixed
> pre-commit. Full offline suite green at commit; 25 product files (19 modified +399/−84, 6 new).
> **Resume point: P15** (Intersection Detail vs-TSN comparison-behavior forward-port).
>
> ---
>
> ▶️ **CR-002 ACCEPTED (Codex, 2026-06-25) — plan amended; ready to resume from P14.** Round 1 = ACCEPTED WITH
> REQUIRED MODIFICATIONS (CR002-RM1–RM7); after the amendment, the **supplemental round-2 review = `ACCEPTED`,
> no further plan changes** (Codex confirmed §A0d + the P14/P15 sections reflect every RM; HEAD `d15216d`, no product diff).
> The forward-port of the v0.17.2–v0.17.8 work (**Intersection Detail (PDF)** + its **vs-TSN comparison
> evolution**, from `origin/main` `068b697` = v0.17.8) **into the refactored v0.18.0 architecture** is accepted.
> Plan (`05`) amended — **§A0d** records CR002-RM1–RM7; §H (order/classification), §I (new **P14**/**P15**, P13
> deps, P11 prereqs), §K, §K2, §N updated — and the phase table + decision log (D26) below reflect it.
> - **Change request:** [`CR-002-claude-proposal.md`](change-requests/CR-002-claude-proposal.md) + [`CR-002-codex-review.md`](change-requests/CR-002-codex-review.md) — **`accepted_with_required_modifications`**.
> - **Method (CR002-RM7):** a **deliberate forward-port** (file-by-file into the refactored sources of truth) — **NOT a `git merge`/rebase** of `origin/main` — guided by `origin/main:docs/refactor-handoff-v0.17.1-to-v0.17.5.md`.
> - **New order after committed P12:** **P14 → P15 → P13 → P11(last)** (P13 moved after the port — it verifies the final **8-report** shape; CR002-RM5). Committed P0–P12 untouched/additive (the port registers a new report family on the existing contracts, it does not reopen them).
> - **Required modifications applied:** SoT = `report_catalog.py` (`reports.py` derived) + `check_report_catalog` parity (RM1); UI mock → `mock.js` not `app.js` (RM2); **dormant `compare_core.context_fill` NOT ported — `compare_core` stays protected/unmodified** (RM3); stable-ID/manifest **append-only** compat gate, 7 export keys keep positions 0–6 (RM4); P14/P15 boundaries distinct (RM5); packaging **proves** `EXPORT_REPORTS` derivation + `version.py` stays **0.18.0** (RM6); no merge/rebase (RM7).
> - **Resume point:** **P14** (next eligible, `pending`). No further user approval needed (dormant `context_fill` omitted — Codex). **Extra pre-commit checks Codex requires for P14/P15 are in the CR-002 review §"Extra verification".**
>
> ---
>
> ▶️ **CR-001 ACCEPTED WITH REQUIRED MODIFICATIONS (Codex, 2026-06-23) — plan amended; ready to resume.**
> The mid-sequence scope change is accepted: **opt into every offline-doable deferral** for **v0.18.0** and **add
> a final phase (P13)** that designs the work-PC validation handoff + the **v0.18.1** close-out. The plan (`05`)
> is amended — §A0c records the RM01–RM08 dispositions; §H/§I/§J/§J2/§K/§K2/§L/§M/§N updated — and the phase
> table + decision log (D25) below reflect it.
> - **Change request:** [`CR-001-claude-proposal.md`](change-requests/CR-001-claude-proposal.md) + [`CR-001-codex-review.md`](change-requests/CR-001-codex-review.md) — **`accepted_with_required_modifications`**.
> - **Two-tier release model (RM02):** v0.18.0 = offline-validated **candidate** (code-complete, offline-provable); v0.18.1 = **field-validated** close-out after the user returns work-PC evidence.
> - **Amended order after committed P9:** **P5b → P7b → P7c → P8c → P9b → P10(expanded) → P12 → P13 → P11(last)**; committed P0–P9 untouched (RM01).
> - **Still hard-deferred (need an explicit separate user decision — RM06):** DPAPI/O2, runtime signature/cert, `compare_core` `min-cost-pairs`.
> - **Status:** **P10 committed** (`a8d9235`, Codex round 4 `PASS`; not pushed) — packaging / deps / updater hardening (CR-001 expanded): the **§J updater set** (fail-closed checksum, zip-slip guard, **mandatory whole-bundle staged re-verify fail-closed**, bounded download retry, revert pagination past 100, `update_helper.log` rotation, windowed death-check, frozen-only webview cache clear), a **hash-pinned reproducible build** (`requirements-build.lock.txt` installed `--require-hashes` + explicit `cryptography` pin + `check_build_env` parity/fail-on-unexpected + `build.ps1` clean-env), and `release.yml` per-variant artifact+`.sha256` enforcement (fails-not-skips). Optional perf measured + dropped (A01). 4 Codex rounds (B01 staged-verify hardening → R01/R02 doc wording) → `PASS`. **69/69** Python + 3 Node + full local frozen `build.ps1 -SelfTest` green. **P12 committed** (`d15216d`, Codex round 3 `PASS`; junction-safe reset + consolidate-overwrite TOCTOU gate at the final replace + M03 ownership marker + the independent PDF expected-row oracle/evidence-capture harness; not pushed). **CR-002 accepted (2026-06-25)** → next eligible **P14** (`pending`; Intersection Detail (PDF) report-family forward-port); new order **P14 → P15 → P13 → P11**. Report: [`phases/P10-claude-report.md`](phases/P10-claude-report.md). See the phase table + log.
>
> See **[Change requests](#change-requests)** below.

**`approved` → implementation in progress** *(CR-001 amendment applied; resuming from P5b — see banner)*. The user **approved** `05-claude-final-plan.md` on **2026-06-21**
(after Codex readiness round 3 = `READY FOR USER APPROVAL`, no blockers) and authorized implementation. Work
proceeds on branch **`refactor/v0.18.0-structural-overhaul`**, **one approved phase at a time**: Claude does
**not** commit a phase until Codex returns `PASS`, and does **not** begin the next phase in the same turn as a
phase commit. One focused commit per approved phase (unless the plan defines another boundary). The planning
folder stays untracked and is **never** staged/committed unless the user later asks.

### Implementation baseline (recorded at branch creation, 2026-06-21)

- **Approved plan:** `docs/planning/v0.18.0/05-claude-final-plan.md`.
- **Starting SHA:** `d2ee35333f3ebd3a070c1adfec893c10d2ffbe58` (`d2ee353`) — local `main` == `origin/main`
  (0 ahead / 0 behind); clean tree apart from the untracked `docs/planning/` workspace.
- **Starting version:** `0.17.1` (`version.py`); tag `v0.17.1` (`9cba578`) is an ancestor of HEAD
  (`git describe` = `v0.17.1-2-gd2ee353`; the 2 extra commits are the post-release docs + `.gitignore`
  housekeeping the baseline already accounts for).
- **Implementation branch:** `refactor/v0.18.0-structural-overhaul` (branched off `main` @ `d2ee353`; **not
  pushed**).
- **Approved phases — 16 headings = 13 release-blocking + 3 conditional** (P5, P7b, P8c). Full names + the
  task graph are in **Final proposed phase table** below; the release-blocking DoD set is P0, PA, P1, P2, P3,
  P4, P6, P7a, P8a, P8b, P9(mock), P10, P11.
- **First phase to implement: P0** — status `pending`. Every later phase remains `pending`.
- **CR-001 amendment (2026-06-23, accepted w/ required modifications):** the 16-heading baseline above is
  **historical**; the amended plan adds **P5b, P9b, P12, P13** and reclassifies **P7b + P8c** to blocking.
  Committed P0–P9 are unchanged (RM01). The amended v0.18.0 offline DoD + the new v0.18.1 acceptance DoD are in
  `05` §K/§K2; the live phase set is **Final proposed phase table** below.

Standing gates **O1/O2/O7** unchanged (all excluded from the DoD). Codex readiness history (Rounds 1–3) and
the three Round-3 cautions are recorded in `05` §A0 + decision log D17–D19 below.

## Change requests

Mid-sequence amendments to the approved plan. A CR pauses phase execution until Codex re-accepts it; on
`ACCEPT`, Claude amends `05-claude-final-plan.md` + the phase table/decision log here, then resumes.

| CR | Title | Raised | Status | Claude rec |
|---|---|---|---|---|
| [CR-001](change-requests/CR-001-claude-proposal.md) ([review](change-requests/CR-001-codex-review.md)) | Maximize offline completion + design a work-PC validation handoff (0.18.0→0.18.1 close-out) | 2026-06-23 | **`accepted_with_required_modifications`** (plan amended) | ACCEPT WITH MODIFICATIONS |
| [CR-002](change-requests/CR-002-claude-proposal.md) ([review](change-requests/CR-002-codex-review.md)) | Forward-port the v0.17.2–v0.17.8 feature work (Intersection Detail PDF + vs-TSN evolution) onto the v0.18.0 structure | 2026-06-25 | **`accepted`** (round 1 = accepted w/ required mods RM1–RM7 → amended; round 2 supplemental = `ACCEPTED`, no further changes; resume from P14) | ACCEPT WITH MODIFICATIONS |

**CR-002 in one line:** `origin/main` moved to **v0.17.8** while the refactor was in flight — ~10 commits
of **one feature** (Intersection Detail (PDF), an exact `highway_log_pdf` parallel, + its localized vs-TSN
comparison evolution; the only `compare_core` touch on `main` is a **dormant** opt-in `context_fill` that
CR-002 does **NOT** port — RM3, `compare_core` stays protected). **Forward-port it into the refactored
structure** (a deliberate structural port following the user's `refactor-handoff-v0.17.1-to-v0.17.5.md`
map — NOT a blind merge) by adding **P14** (report-family plumbing) + **P15** (vs-TSN comparison evolution),
sequenced **P12 → P14 → P15 → P13 → P11**; committed P0–P12 untouched/additive; every shipped canary
re-proven + the new report's **live** acceptance folds into the existing **v0.18.1 P13** kit. **Codex ACCEPTED
WITH REQUIRED MODIFICATIONS (2026-06-25)** — CR002-RM1–RM7 applied in `05` (§A0d/§H/§I/§K/§K2/§N): SoT =
`report_catalog.py`; mock → `mock.js`; **dormant `compare_core.context_fill` NOT ported (compare_core stays
protected)**; stable-ID append-only gate; P14/P15 distinct; packaging proves `EXPORT_REPORTS` derivation +
`version.py` stays 0.18.0; no merge/rebase. New order **P12 → P14 → P15 → P13 → P11**. Resume from **P14**.

**CR-001 in one line:** opt into every *offline-doable* deferral (P5b comparator, P7b full depth, P8c code,
P9b deeper UI split, expanded P10, a new P12 audit-hardening phase) for **v0.18.0**, and add a final **P13**
that designs the work-PC evidence kit + the **v0.18.1** acceptance plan; keep only the hard-blocked items
deferred (O2/DPAPI trade-off, code-signing cert, `compare_core`-locked `min-cost-pairs`). DoD becomes two-tier
(0.18.0 offline-provable · 0.18.1 work-PC-validated). **Codex accepted with required modifications RM01–RM08;
`05` amended (§A0c + §H/§I/§J/§J2/§K/§K2/§L/§M/§N) + the phase table/D25 below; ready to resume from P5b.**

## Roles

- **Claude** — planner and (eventual) sole implementer.
- **Codex** — independent investigator and adversarial reviewer (peer analysis, verified not authority).

## Planning artifacts

| File | Owner | Purpose |
|---|---|---|
| `00-coordination.md` | Claude | This file — status, roles, decisions, final phase table |
| `01-claude-investigation.md` | Claude | Claude investigation (structure-weighted) |
| `02-codex-investigation.md` | Codex | Codex investigation (correctness-contract-weighted) |
| `03-claude-draft-plan.md` | Claude | Draft Plan v1 |
| `04-codex-plan-review.md` | Codex | Codex adversarial plan review — Round 1 (NOT READY; resolved in `05`) |
| `05-claude-final-plan.md` | Claude | **Claude Final v0.18.0 Plan — APPROVED 2026-06-21** (incl. §A0 readiness-blocker table R1–R3 + §J2 audit dispositions) |
| `06-codex-readiness-review.md` | Codex | **Codex readiness review — Rounds 1–3 (Round 3 = `READY FOR USER APPROVAL`, no blockers)** |

## Decision log

| ID | Decision | Rationale |
|---|---|---|
| D1 | **Contracts-first sequencing** — F1–F5/F9 fixed + characterized before structural decomposition. | F4 proves an implicit contract already drifted; decomposition is safer once contracts are explicit. |
| D2 | **F4 accepted/elevated** (aggregate vs-TSN counts read wrong layout). | `matrix:874`/`day_matrix:355` hardcode `has_route=True` vs `has_route=False` adapters. |
| D3 | **`compare_core` structurally untouched** (helpers + counts live OUTSIDE it). | Both investigations + R1-R12; regression-lock risk ≫ reward. |
| D4 | **Deeper `app.js` split gated on O1**; mock extraction proceeds regardless. | Current GUI is a documented stopgap; R1-N03. |
| D5 | **Registry → stable string keys** with manifest back-compat. | Index coupling is a real data-correctness vector (F7). |
| D6 | **Auth-at-rest = atomic write + ACL; DPAPI gated on O2.** | DPAPI is user+machine-scoped → breaks `storage_state_is_portable`. |
| D7 | **Do not revive refutation-rejected audit items as defects.** | Both agree; `paths` import-time mutation is intentional. |
| D8 | **No framework rewrite** (no asyncio/plugin/one-class-per-action/ES-modules). | Both warn against it. |
| D9 | **Orthogonal outcome model** — `completion` + `artifact` fields, producer-owned, never parsed from `summary_lines`. | R1-B01/D01; a partial consolidation, a preserved old workbook, and a valid no-data export are independent axes. |
| D10 | **Engine extraction follows the verified acyclic DAG**: leaves → `browser_channels` ∥ `auth_nav` → `report_nav`/`edge_device` → `session`. **O3 race recorded non-reproducible; no global site lock.** | Verified calls (`edge_device`→`navigate_with_auth`+`launch_browser`); `ActiveEnvCheckWorker`/`EnvScanWorker` use `set_thread_site`. R1-B03/D04/R07. |
| D11 | **Exact-artifact gate (PA) moved early** via a real-exe `--self-test` flag; precedes broad module/UI extraction. | R1-B04/S02; `-SelfTest` builds a different exe today. |
| D12 | **`report_catalog` = report metadata only**; packaging reachability + test oracles stay independent. | R1-B05/D02/D05; deriving the oracle from the code under test is self-validating. |
| D13 | **4-tier stable IDs** (report-family / export-op / consolidation-op / comparison-op); manifest v1/v2 **rejects-not-drops** invalid keys; `batch_job.json` persists the export-op key. | R1-B06/R04; 7 export vs 8 consolidate vs 15 compare rows aren't isomorphic. |
| D14 | **Phases classified release-blocking / conditional / deferrable; the DoD excludes conditional work and ALL work-PC acceptance.** P7→P7a/P7b, P8→P8a/P8b/P8c, P10→PA(early)+P10(later). | R1-P01; the code DoD must be satisfiable from CI/offline alone. |
| D15 | **One cache envelope, one rebuild; P2 depends on P1.** | R1-R15/S01/S03; avoid two user-visible cache migrations. |
| D16 | **Dropped at Codex's request:** `paths`-init rewrite (R05), bounded worker queue (A02), CI trigger change (A05), `run_compare`-returns-counts, `_safe_join`/`full_snapshot` (N02). **Deferred:** DPAPI (O2), runtime signature abstraction (A03), destination-ownership marker (M03→roadmap). | Repository-backed; avoids ceremony/dead abstraction. |
| D17 | **Readiness Round 1 resolved (RR1-B1/B2 + 3 cautions).** The full Phase-3 audit disposition is now **in the plan** (`05` §J2 — every still-open finding → phase or deferral); the updater is **fail-closed** (requires a verified checksum before extraction; never size-only). `min-cost-pairs-greedy-not-optimal` is **deferred (repo-backed)** because it lives inside the regression-locked `compare_core`. | Architecture was approved; only scope records needed correction. No new user decision arose. |
| D18 | **Readiness Round 2 resolved (RR2-B1 + 3 cautions).** The **PDF Highway Log trio** is now dispositioned explicitly in §J2 — verified **reporting-only** at HEAD (`consolidate_tsmis_highway_log_pdf.py:294`; `check_tsmis_pdf_reconcile.py:78–81` asserts banners only), so it is **not** described as closing parser correctness: the dropped-line skip is Resolved (logged) and **P1 escalates the `stats` to a producer-owned `partial` completion**; the stale-geometry **emit** + an independent expected-row oracle are **Deferred** (roadmap; the audit's geometric cross-check found 0 real occurrences over 756 PDFs). `wait_js` validation stays in P8b as the single explicitly-permitted behavior change; every P8c item defaults to **Deferred without O7**; **`mock.js` owns the mock boot**. | v0.17.0 reporting made the silent failures visible; v0.18.0's outcome contract escalates them to `partial` **without touching the locked row-emit logic**; the completeness oracle needs real PDFs (work-PC). No new user decision. |
| D19 | **Readiness Round 3 = `READY FOR USER APPROVAL` (no blockers); 3 cautions folded in.** RR3-C1: without O7 the **P8c behavior-change commits are omitted from v0.18.0 entirely** (not shipped dormant). RR3-C2: P0's two small behavior fixes (side-label preservation, Ramp Summary schema guard) ship as **isolated, characterized commits** (P0's "no behavior change" line carved out). RR3-C3: the **released** cache-envelope `schema_version` carries the P2 `input_fingerprint` — P1+P2 ship together, **one** rebuild. | Cautions only — no redesign, no new user decision. The plan is ready for the user's approval; implementation is unstarted/unauthorized. |
| D20 | **Plan APPROVED by the user (2026-06-21); implementation authorized.** Branch `refactor/v0.18.0-structural-overhaul` off `main` @ `d2ee353` (v0.17.1 baseline, origin-synced). Phase-at-a-time; **no phase commit until Codex returns `PASS`**; the next phase never starts in the same turn as a commit; planning folder stays untracked. | Starting-point verification passed (clean tree, on `main`, v0.17.1 merged, `origin/main` in sync). First phase **P0** pending. |
| D21 | **P0 review round 1 (Codex `BLOCKED`) resolved; the duplicate-late terminal-delivery *fix* is sequenced to P7a (coordination-approved).** CT-10 (`check_worker_lifecycle.py`) is rewritten to a genuine **per-worker terminal contract** (15 worker/terminal combinations — success/cancel/expected-fail/unexpected-fail) + queue-advance, and now **reproduces and LOCKS** the duplicate-late-active-successor gap: a straggler terminal clobbers an already-started successor because `_end_task` clears state unconditionally. The **fix** (exactly-once terminal delivery) is **P7a's** TaskCoordinator job (R1-R06 / R1-R14, "exactly-once transitions") — not a 3rd P0 behavior delta. Also: import-direction guard corrected (descends module-scope `try`/`if`, keeps self-edges, 6 detection self-tests); gui-bridge §8 silent-drop statements + all 10 worker anchors (incl. EnvScanWorker) corrected; `check_no_misspelling` added to the documented CI list; P0 cold-start/matrix-snapshot baselines recorded with R1-A01 metadata. | Codex's `BLOCKED` concerned CT-10 honesty + tooling correctness, not architecture. Deferring the exactly-once fix to P7a matches the approved plan's phase ownership; P0 stays "diagnostics + 2 named behavior deltas." No new user decision. |
| D22 | **P0 review round 2 (Codex `BLOCKED`) resolved.** CT-10 rewritten to **producer-path** coverage — runs each gate-owning worker's real `run()` with stubbed seams, asserts **exactly-one-terminal** per outcome, then feeds it through `_handle` for gate release (11 worker classes / 21 producer scenarios, incl. the Codex-required **EnvCheckWorker→`env_shot`** + **EnvScanWorker→`env_access_done`**) + 11 payload-encoded cancel/error variants; the D21 active-successor known-gap is retained (**P0 does NOT implement the P7a exactly-once fix**). The protocol "master map" is completed (4 missing kinds added; discrepancy note → 8 omitted; table now maps **all 27** worker-posted kinds). A true **process-cold** baseline incl. `import gui_api` (~1.45 s median, matching Codex's 1160–1561 ms) is recorded via committed tool `build/measure_baselines.py`; the 9 ms figure is relabelled warm. | Round 2 accepted the round-1 fixes + D21; it re-blocked on producer-path coverage, protocol completeness, and cold-start labelling — all coverage/tooling/measurement, no architecture change. No new user decision. |
| D24 | **PA in progress — the exact-artifact gate runs the COMPARE/EXACT shipped windowed exe, so the comprehensive self-test body is extracted to a shipped `scripts/self_test.py`.** `gui_main --self-test` and `build/full_smoke.py` both delegate to `self_test.run(emit)`; `build.ps1 -SelfTest` is pivoted to build the **windowed** `TSMIS Exporter.exe` (not a separate console `full_smoke` exe), copy docs + prune, then run `TSMIS Exporter.exe --self-test` as the gate. This is the only way to satisfy BOTH "the exact shipped exe boots/imports" (R1-B04/D11) AND the PA Protected contract "the `excludes`/prune behavior proven by `-SelfTest` (never weaken)" — the prune-safety proof (browser+pdf+openpyxl exercise) must run on the exact windowed artifact, not a different console exe. Windowed exes have no stdout, so output is mirrored to the log + an optional `TSMIS_SELFTEST_OUT` file for CI diagnosability. `full_smoke.py` stays as the venv-only tool (its docstring use #1). | Mechanism enabling the plan's PA design; no scope expansion (the extraction is the means to "run the same self-test full_smoke performs" on the exact exe). No new user decision. |
| D23 | **P0 review round 3 (Codex `BLOCKED`, narrowed to one worker) resolved.** Codex confirmed P0-R02 + P0-R03 resolved; the sole remaining blocker was **LoginWorker** missing from CT-10's producer path (with an inaccurate "covered by variants" claim). LoginWorker is now producer-tested **offline** — 6 scenarios (`login_saved` / `login_device_ok` / `login_failed` / `cancelled` / expected-error / unexpected-error), each emitting exactly one terminal that frees the `login` gate via `_handle` — driven by a fake Playwright + an `_AlwaysDone` event + stubbed `new_login_context`/`is_logged_in`/`_save_state` (no browser, no auth-file write), with the Edge fallback isolated via `_try_edge_persistent_login`/`storage_state_is_portable`. The inaccurate exclusion docstring is removed. CT-10 now covers **all 12 gate-owning workers / 27 producer scenarios / 75 assertions — no exclusions.** | Codex proved offline producer-testing was feasible; no live browser / auth file / P7a fix needed. No new user decision; no product code changed this round (CT-10 check only). |
| D25 | **CR-001 accepted with required modifications (Codex, 2026-06-23) → two-tier release model + offline-doable opt-in.** **v0.18.0 = the offline-validated *candidate*** (code-complete, every phase offline-provable; MAY carry P8c live-path code); **v0.18.1 = the field-validated close-out** after the user runs v0.18.0 on the work PC and returns evidence (the P13 kit). Offline-doable deferrals opted in: **P5b** (comparator driver), **P7b** (full depth), **P8c code** (fixture-first, revertible RC commits; acceptance → v0.18.1), **P9b** (deeper UI split, no framework), **expanded P10** (flipped updater items + perf), new **P12** (junction/TOCTOU/M03 + the PDF oracle *harness*), new **P13** (work-PC handoff). Committed P0–P9 untouched (RM01). Hard-deferrals kept out pending an **explicit separate user decision** (RM06): DPAPI/O2, runtime signature/cert, `compare_core` `min-cost-pairs`, the D16-dropped set. Plan amended in `05` §A0c + §H/§I/§J/§J2/§K/§K2/§L/§M/§N. | The user removed the resourcing constraint + granted the work-PC validation window (answering **O7** for v0.18.1) and the maintainability mandate (answering **O1** toward the deeper split, no framework); Codex required RM01–RM08 (don't reopen committed phases; v0.18.0 ≠ field-validated; P8c revertible-not-flagged; honest PDF/parser claims; credential-safe evidence kit; bounded P7b/P9b decomposition; corrected counts — 65 `check_*.py`). **No further user approval needed** for the amended plan provided the hard-deferrals stay deferred. |
| D26 | **CR-002 accepted with required modifications (Codex, 2026-06-25) → forward-port the v0.17.2–v0.17.8 feature work into the v0.18.0 structure.** `origin/main` reached **v0.17.8** (`068b697`) while the refactor sat on its v0.17.1 branch point; the divergence is **one feature** — **Intersection Detail (PDF)** (an exact `highway_log_pdf` parallel) + its **vs-TSN comparison evolution**. Add two new blocking phases — **P14** (report-family plumbing) + **P15** (vs-TSN comparison evolution) — sequenced **P12 → P14 → P15 → P13 → P11**; P13 moved after the port (verifies the final **8-report** shape). **Method: a deliberate forward-port into the refactored sources of truth — NOT a `git merge`/rebase** (CR002-RM7), guided by `origin/main:docs/refactor-handoff-v0.17.1-to-v0.17.5.md`. Plan amended in `05` §A0d + §H/§I/§K/§K2/§N. | Without the port v0.18.0 either **drops a shipped report** (data loss for v0.17.8 users) or reintroduces the **old structure** via a blind merge (defeating the overhaul). Codex required **CR002-RM1–RM7**: port into `report_catalog.py` (not the pre-refactor registry); UI mock into `mock.js` not `app.js`; **omit the dormant `compare_core.context_fill` — keep `compare_core` protected** (it has no live v0.17.8 user; porting it needs a separate explicit decision); stable-ID/manifest **append-only** compat gate (7 export keys keep positions 0–6); distinct P14/P15 boundaries with P13 after both; packaging proves `EXPORT_REPORTS` derivation + `version.py` stays **0.18.0**; no merge/rebase. **No further user approval needed** provided the dormant `context_fill` stays omitted. Committed P0–P12 untouched/additive (RM01-style). **Supplemental round-2 review (2026-06-25) = `ACCEPTED`** — Codex confirmed the amended `05`/`00` reflect every RM (no further plan text required); resume from P14. |

## Remaining user decisions / blockers

Only three items require a genuine user decision; everything else is resolved. **Codex Readiness Round 1
introduced no new user decision** (its two blockers were scope-record fixes, now done). None of O1/O2/O7
blocks starting the release-blocking phases (P0/PA/P1/P2/P3/P4/P6/P8a) — they gate only specific
later/conditional work.

| ID | Question | Gates | Default if unanswered |
|---|---|---|---|
| O1 | Is the planned full GUI replacement imminent? | P9 deeper split, P7b depth | Mock-separation only; deeper split deferred |
| O2 | Is auth-file **portability** still required? | P6 DPAPI vs ACL | ACL only (portability preserved) |
| O7 | Work-PC acceptance availability/timing? | P8c inclusion | P8c deferred to a point release if no acceptance window |

*Resolved this round:* O3 (non-reproducible — no lock), O4 (folded into P1 as a verify task), O5 (keep
the `check_*.py` model + add deterministic contract tests), O6 (node-driver removal = an optional P10 spike).

**CR-001 (2026-06-23) answers O1 + O7 (D25):** **O1** → the deeper GUI split proceeds **now** (P7b full depth +
P9b), within the no-framework guardrail (RM08); **O7** → the work-PC acceptance window is **granted for v0.18.1**,
so P8c ships its offline-proven code in v0.18.0 and is accepted in v0.18.1. **Only O2 (DPAPI) remains a genuine
open user decision**, alongside the two other hard-deferrals (runtime signature/cert, `compare_core`
`min-cost-pairs`) — each opt-in needs an explicit separate "yes" (RM06).

## Final proposed phase table

Statuses: `pending` · `in_progress` · `awaiting_review` · `changes_requested` · `approved` · `committed` · `blocked` · `complete`.

| ID | Name | Class | Depends on | Status |
|---|---|---|---|---|
| P0 | Safety net + diagnostics + doc-drift + dispatch defaults | blocking | — | committed |
| PA | Exact-artifact packaging safety gate (early) | blocking | P0 | committed |
| P1 | Completion/outcome contract + F1/F3/F4 + frontend payload | blocking | P0 | committed |
| P2 | Transactional artifact lifecycle (F2/F9/F5) | blocking | P1 | committed |
| P3 | Stable-ID taxonomy + manifest v1/v2 migration | blocking | P0 | committed |
| P4 | Report metadata catalog + `.bat` fix + parity checks | blocking | P3, PA | committed |
| P5 | Report-family DRY — family-1 `tsn_load` factory | blocking (family-1 done) | P3, P4 | committed |
| P5b | TSN comparison-driver DRY (comparator remainder) | blocking (CR-001) | P4, P5 ✓ | committed |
| P6 | Persistence hardening (narrowed; DPAPI→O2) | blocking core | P0 | committed |
| P7a | GUI state owner + protocol + lifecycle + enum SSOT | blocking | P1, PA | committed |
| P7b | GUI mechanical endpoint extraction (gui_win32 + compare-unify) | blocking (CR-001) | P7a ✓ | committed |
| P7c | GUI Matrix feature-endpoint grouping | blocking (CR-001/P7b-R01) | P7b ✓ | committed |
| P8a | Engine leaf extraction (shim, behavior-neutral) | blocking | PA | committed |
| P8b | Engine mechanical movement (acyclic DAG, shim) | blocking | P8a | committed |
| P8c | Engine behavior changes (select/CDP/cancel/swallows) — v0.18.0 code, accept v0.18.1 | blocking code (CR-001) | P8b ✓ + fixture | committed |
| P9 | Frontend mock separation | blocking (mock) | P4, PA | committed |
| P9b | Frontend deeper modularization (renderer merge + app.js split; no framework) | blocking (CR-001) | P9 ✓ | committed |
| P10 | Packaging / deps / updater hardening (expanded: flipped updater items + perf) | blocking | PA, P4 | committed |
| P12 | Residual offline audit hardening + evidence harnesses | blocking (CR-001) | P2 ✓, P3 ✓ | committed |
| P14 | Intersection Detail (PDF) report-family forward-port (HL-PDF parallel) | blocking (CR-002) | P3 ✓, P4 ✓ | committed |
| P15 | Intersection Detail vs-TSN comparison-behavior forward-port (v0.17.8) | blocking (CR-002) | P14, P5b ✓ | committed |
| P13 | Work-PC validation handoff + v0.18.1 close-out plan (FINAL) | blocking (CR-001/CR-002) | P8c, P10, P12, **P14, P15** | committed |
| P11 | Documentation + audit/roadmap reconciliation (LAST) | blocking | amended v0.18.0 blocking set (+ P14/P15) | committed (`375b48c`) |

**v0.18.0 offline DoD set (amended CR-001 + CR-002):** P0, PA, P1, P2, P3, P4, P5b, P6, P7a, P7b, P7c, P8a, P8b,
P8c(code), P9, P9b, P10, P12, **P14, P15 (CR-002)**, P13, P11. (CR-002 order after committed P12:
**P14 → P15 → P13 → P11**.)
**Excluded from the v0.18.0 DoD → v0.18.1 (`05` §K2):** all **work-PC acceptance** + the evidence-driven
PDF/parser correctness fixes. **Hard-deferred (explicit separate user decision — RM06):** DPAPI/O2, runtime
signature/cert, `compare_core` `min-cost-pairs`, and the D16-dropped set.

## Phase implementation log

| Phase | Status | Baseline | Claude report | Notes |
|---|---|---|---|---|
| **P0** | `committed` (Codex round 4 `PASS`) | `d2ee353` | [`phases/P0-claude-report.md`](phases/P0-claude-report.md) (+ §Remediation R1, R2, R3) | 9 files modified + 5 new `build/` files (4 `check_*.py` + the `measure_baselines.py` tool); **48/48** golden checks green (44→48), byte-compile + `node --check` + `#mock` boot verified. **Codex rounds 1–4: rounds 1–3 `BLOCKED` → all findings resolved; round 4 = `PASS`.** R1: import-guard + self-tests, gui-bridge §8 silent-drop + anchor fixes, `check_no_misspelling` documented, baselines recorded. R2: CT-10 → **producer-path** + payload variants (D21 known-gap retained); protocol table completed (all 27 kinds); true process-cold baseline. R3: **LoginWorker producer-tested offline** (6 scenarios) → CT-10 covers **all 12 gate-owning workers / 27 scenarios / 75 assertions, no exclusions**. **Committed as the 3 plan-mandated isolated boundaries (RR3-C2 / P0-A02):** `a009b6d` side-label · `36ce1c4` ramp-summary guard · `4bbee65` diagnostics/docs/tooling. Planning folder not staged; not pushed. Next eligible phase: **PA** (`pending`). |
| **P1** | `committed` (Codex round 11 `PASS`) | `65aef98` | [`phases/P1-claude-report.md`](phases/P1-claude-report.md) | Completion/outcome contract + F1/F3/F4 + frontend. **Verification green (offline):** new `outcome.py` (orthogonal completion/artifact, producer-owned predicates) + additive `events.py` fields; **F1** store-promote gate (partial/failed/cancelled keep last-good, only `complete` promotes); **F3** `_consolidate_store_folder` returns its result + `consolidate_and_compare_tsn`/`_ensure_consolidated` honor it (raise the consolidator's own message, cache untouched); **F4/O4** `read_counts` auto-detects layout from the workbook header (fixes aggregate vs-TSN **and** cross-env 0-diff); 6 consolidators set producer-owned `completion`+`skipped/failed_inputs` (HL-PDF/TSN escalate drops→partial, RR2-B1/D18); new `cache_envelope.py` versions the 3 matrix/by-day caches (one forward rebuild); `gui_api` summary + `run_ended` carry completion/artifact; `app.js renderCompletion` branches on completion (skipped-only now "incomplete", not green) + mock parity; matrix `ok`/auto-chain gated on complete. **CT-1/CT-2/CT-3/CT-14** added → **54/54** suite + byte-compile + `node --check` + `check_app_modules` + YAML all green; **`#mock` verified** all completion states (no console errors). `compare_core`/updater/auth untouched. **Owed:** work-PC partial-keeps-last-good (§M; not DoD). Files: `outcome.py`+`cache_envelope.py`+4 CTs new; `events`/`gui_worker`/`gui_api`/`matrix`/`day_matrix`/`app.js`/6 consolidators/`app.spec`/`checks.yml` modified. **Codex round 1 = `BLOCKED`** (4 blocking + 1 required, all real — the round-1 checks were green because they didn't exercise the failing orchestration paths) → **all 5 resolved (round-1 remediation):** **P1-B01** `read_counts` now locates Status/Diffs by **header label** (the flat Ramp/Intersection Summary sheets DO start with 'Route' — A1 was not a layout signal); **P1-B02** `BatchWorker` retains results + marks an env done ONLY if every report complete (partial→pending) + `batch_done.completion`; **P1-B03** in-store `exists` anomaly forced non-promotable (failed, last-good kept); **P1-B04** new `outcome.reduce_completion` reduces over per-report completions (complete+no_data→partial, never green) + `aborted`; **P1-R01** partial consolidation propagated into the comparison result + a `completion` field in the cache records + `_cmp_state`. **New regression tests that fail on the pre-fix code:** `check_batch_outcome` (real `BatchWorker`/`_run_specs` producer paths, B02/B03), rebuilt `check_read_counts_layout` (flat-'Route'-in-A + end-to-end into the cache, B01), expanded `check_consolidate_outcome` (F3 raise + R01 flag durability), `check_export_summary_outcome`/`check_outcome_contract` (reducer). **55/55** suite + byte-compile + node + app_modules + YAML + diff-check green. Round-1 remediation appended to the report. **Codex round 2 = `BLOCKED`** (P1-B01 + P1-B04 confirmed Resolved; 2 blocking + 1 required remained, all reproduced) → **all 3 resolved (round-2 remediation):** **P1-B02** `start_batch_export`/`resume_batch` now CLEAR `_last_batch_outcome` + `_end_task` defaults a missing batch outcome to FAILED, so a failed run (error path via `_on_error`) can't publish the previous run's success; **P1-B03** `_run_specs` gates the B2 auto-consolidate so a STORE refresh consolidates ONLY when promoted (`artifact==PROMOTED`) — a partial/rejected refresh no longer rebuilds a derived artifact from stale last-good (non-store dated-run behavior unchanged); **P1-R01** producer completion now persisted in an **mtime-guarded sidecar** beside each consolidated workbook (recovered on REUSE when `cres is None`), `_ensure_consolidated` returns `(path, completion)` so the self comparison reduces both sides, `day_matrix` threads `completion` into the by-day cache, and `app.js mxCellContent` renders a partial cell as a distinct **`mx-partial`** (amber + inset border + "inputs incomplete", + `app.css` rules + a legend key) instead of the green `mx-match`. **New regression tests that fail on the pre-fix code (verified by revert/run/restore):** `check_batch_outcome` consecutive-run leak (real GuiApi lifecycle) + the partial-refresh auto-consolidate gate; `check_consolidate_outcome` sidecar round-trip/mtime-guard + reuse-keeps-partial + self-one-partial + snapshot serialization; **new `check_mx_partial_render.js`** (Node `vm` renderer guard, wired blocking into `checks.yml`). **55/55** Python suite + the Node renderer check (56) + byte-compile + `node --check` + app_modules + all-workflow YAML + diff-check green; **`#mock` re-verified** the live `mx-partial` cell (distinct amber bg + inset border vs green `mx-match`, legend key, no console errors). `compare_core`/updater/auth untouched. Round-2 remediation appended to the report. **Codex rounds 3 & 4 = `BLOCKED`** (round 4 re-states round 3; P1-B01/B03/B04 confirmed Resolved, P1-R01 partially) → **all 3 open findings resolved (round-3/4 remediation):** **P1-B05** every incomplete-capable TSN producer (`tsn_load_ramp_summary`/`tsn_load_intersection_summary` → partial+`skipped_inputs`; `consolidate_tsn_highway_sequence` → partial+`failed_inputs`) now sets producer-owned completion; `tsn_library` persists+exposes it via `resolve`; `build_comparison` AND `build_day_cell` reduce the TSN side → both matrices flag a partial TSN cell; **P1-R01** outcome persistence moved to a shared **`consolidation_meta`** boundary (new leaf module: atomic tmp+replace write, validated/safe-degrade read — corrupt→conservative partial, never a `ValueError`, no-sidecar→legacy complete) called by EVERY persistent writer (matrix store, `_auto_consolidate`, `ConsolidateWorker`, TSN-library) so no writer bypasses it; **P1-B02** `_on_error` now reduces a mid-batch fatal error to **partial** when ≥1 env completed (resumable), reserving failed for zero. **New regression tests that fail on the pre-fix code (revert/run/restore-verified):** new **`check_tsn_outcome.py`** (producers + library persist/resolve + Everything & by-day reduce, end-to-end; wired into `checks.yml`); `check_consolidate_outcome` (fail-safe read incl. Codex's non-numeric-mtime `ValueError` repro, atomic write, `ConsolidateWorker`/`_auto_consolidate` persist → matrix-reuse-reads-partial; fixed a `consolidated_store_path` parent-collision test-isolation bug); `check_batch_outcome` (two-env complete-then-fatal → partial + resumable, real `BatchWorker`→`GuiApi` replay). **`app.spec` APP_MODULES += `consolidation_meta`** (F6). **56/56** Python suite (55+`check_tsn_outcome`) + Node renderer + byte-compile + `node --check` + app_modules + import-direction + all-workflow YAML + diff-check green. No UI files changed this round. `compare_core`/updater/auth untouched. Round-3/4 remediation appended to the report. **Codex round 5 = `BLOCKED`** (P1-B01/B02/B03/B04 Resolved, P1-R01 substantially) → **both remaining resolved (round-5 remediation):** **P1-B05** `matrix.consolidate_tsn_pdfs` now **raises** on a failed/no-data producer result so the legacy `MatrixTsnConsolidateWorker` reports a not-refreshed terminal (`errors=1`, no "TSN workbook ready") + keeps the prior workbook (a partial set stays usable+flagged); **P1-R01** the console `cli.run_consolidate_cli` now routes through `consolidation_meta` (console/`.bat` bypass closed), and `consolidation_meta` is now **fail-safe** — `write_outcome` cleans the `.tmp` and removes a non-complete workbook when publication fails (so a partial can't be reused-as-complete; a complete artifact is safely kept), and `read_completion` reads a present-but-unreadable sidecar as conservative `partial` (not absent-legacy). **New regression tests in `check_consolidate_outcome` (revert/run/restore-verified):** the REAL `MatrixTsnConsolidateWorker.run` over a prior workbook + error producer (errors=1, no "ready", prior unchanged); the REAL `run_consolidate_cli` persists; injected `os.replace` `PermissionError` for partial (no `.tmp`, workbook removed) vs complete (kept); a present-but-unreadable sidecar → partial. **56/56** Python suite + Node renderer + byte-compile + `node --check` + app_modules + import-direction + all-workflow YAML + diff-check green. No UI files changed this round. `compare_core`/updater/auth untouched. Round-5 remediation appended to the report. **Codex round 6 = `PASS WITH FIXES`** (no blockers; P1-B05 + all prior Resolved; one narrow P1-R01 required) → **resolved (round-6 remediation):** **P1-R01** `consolidation_meta.write_outcome` now **returns an observable bool** (False only when a partial artifact's flag couldn't be published) and, on publication failure, leaves a **durable conservative `partial` marker** (`_mark_untrusted`) so a partial workbook that ALSO can't be unlinked (Windows lock) still reads `partial` — not a false green; every announcing caller honors the return (`run_consolidate_cli` fails + `exit(1)`, `ConsolidateWorker` emits a degraded `error` terminal, `_auto_consolidate` logs the discard, `consolidate_tsn_pdfs` raises). **New `check_consolidate_outcome` round-6 tests (revert/run/restore-verified):** observable True/False; publication+unlink failure → False + preserved workbook + `read_completion` partial (no false-green); COMPLETE publish-fail → True/complete; CLI non-zero exit + no success summary; `ConsolidateWorker` `error` terminal not `consolidate_done`. **56/56** Python suite + Node renderer + byte-compile + `node --check` + app_modules + import-direction + all-workflow YAML + diff-check green. No UI files changed this round. `compare_core`/updater/auth untouched. Round-6 remediation appended to the report. **Codex round 7 = `PASS WITH FIXES`** (no blockers; P1-B01..B05 Resolved; one narrow P1-R01 required) → **resolved (round-7 remediation):** **P1-R01** closed the **three-way-failure** false-green (atomic publish + `_mark_untrusted` marker + workbook unlink all fail) — `write_outcome` now RETAINS the already-written valid `.tmp` as a last-resort sentinel (cleaning it only when there's no false-green risk) and `read_completion` falls back to it on a missing final sidecar → `partial`, never sidecar-less `None`; and the two ignored-return callers now honor `False` (`matrix._consolidate_store_folder` raises; `tsn_library.build_consolidated` returns an error result). **New regression tests (revert/run/restore-verified):** `check_consolidate_outcome` — the exact three-way failure (False + workbook preserved + no final sidecar + `.tmp` retained + `read_completion` partial) + subsequent matrix reuse records partial + `_consolidate_store_folder` raises; `check_tsn_outcome` — `build_consolidated` returns an error result. **56/56** Python suite + Node renderer + byte-compile + `node --check` + app_modules + import-direction + all-workflow YAML + diff-check green. No UI files changed this round. `compare_core`/updater/auth untouched. Round-7 remediation appended to the report. **Codex round 8 = `PASS WITH FIXES`** (no blockers; P1-B01..B05 Resolved; one narrow P1-R01 required + a non-blocking P1-A01) → **resolved (round-8 remediation):** **P1-R01** closed the **write-stage** false-green — when `open(tmp)`/`json.dump` itself fails (NO `.tmp` to retain) and marker+unlink also fail, `write_outcome` now **quarantines** the workbook (renames it aside → canonical path MISSING → resolver rebuilds; data kept at `<workbook>.unverified`), with a `log.critical` all-locked floor; the failure path is a full ladder (remove → marker → retain-usable-`.tmp` → quarantine → log). **P1-A01** (applied) a shared `_read_sidecar` validates the `.tmp` sentinel like the final sidecar (schema/vocab/type/**mtime**) so a demonstrably-stale sentinel is ignored (`None`) instead of presence-only `partial`; `_mark_untrusted` wording/log corrected. **New regression tests (revert/run/restore-verified):** `check_consolidate_outcome` — write-stage failure (path-filtered `builtins.open` denial + marker/unlink fail) → False + no `.tmp` + workbook quarantined + matrix reuse raises not-refreshed; a stale `.tmp` ignored (`None`) vs a current one (`partial`). **56/56** Python suite + Node renderer + byte-compile + `node --check` + app_modules + import-direction + all-workflow YAML + diff-check green. No UI files changed this round. `compare_core`/updater/auth untouched. Round-8 remediation appended to the report. **Codex round 9 = `PASS WITH FIXES`** (no blockers; all prior Resolved; one narrow P1-R01 sentinel-integrity) → **resolved (round-9 remediation):** **P1-R01** the fallback ladder's retain-`.tmp` rung now accepts a sentinel **only when it itself reads `partial`** (`_read_sidecar(tmp) == outcome.PARTIAL`) — a valid current `complete` (or stale) sentinel is rejected as incompatible and the ladder continues to **quarantine**, so a pre-existing `complete` temp can no longer certify a newly-failed `partial` write as green. **New `check_consolidate_outcome` test (revert/run/restore-verified):** a planted CURRENT `complete` `.tmp` + a forced-failing partial write (write-only-deny `builtins.open` keeps the tmp readable, marker + deletes fail) → False + workbook quarantined + `read_completion` never the false `complete` + the shared `consolidate_and_compare_tsn` (Everything + by-day) raises not-refreshed. **56/56** Python suite + Node renderer + byte-compile + `node --check` + app_modules + import-direction + all-workflow YAML + diff-check green. No UI files changed this round. `compare_core`/updater/auth untouched. Round-9 remediation appended to the report. **Codex round 10 = `PASS WITH FIXES`** (no blockers; all prior Resolved; one narrow P1-R01 read-precedence) → **resolved (round-10 remediation):** **P1-R01** `read_completion` now reconciles the final sidecar and the retained `.tmp` **conservatively** — a `partial` `.tmp` (the most-recent record of a failed partial publication) **dominates** a `complete` final that only validates because the workbook was overwritten within the 1s mtime tolerance / on a coarse FS; both stay mtime-validated so a genuinely-stale `.tmp` can't override a current `complete`. **New `check_consolidate_outcome` test (revert/run/restore-verified):** a planted CURRENT `complete` final + a forced-failing partial write (deny the final overwrite, allow the `.tmp`; replace/marker/deletes fail) → both files persist, `write_outcome` False, `read_completion` returns partial (tmp dominates), and the shared `consolidate_and_compare_tsn` (Everything + by-day) records partial — never green. **56/56** Python suite + Node renderer + byte-compile + `node --check` + app_modules + import-direction + all-workflow YAML + diff-check green. No UI files changed this round. `compare_core`/updater/auth untouched. Round-10 remediation appended to the report. **Codex round 11 = `PASS`** (no blocking/required findings; P1-B01..B05, P1-R01, P1-A01 all resolved). **Committed `e47b700`** "feat: add completion/outcome contract (F1/F3/F4) + frontend payload" — the **31** intended product files staged by explicit path (3 new modules `outcome`/`cache_envelope`/`consolidation_meta` + 7 new `build/check_*` + 21 modified: `events`/`gui_api`/`gui_worker`/`matrix`/`day_matrix`/`cli`/`tsn_library`/`tsn_load_*`/6 consolidators/`ui/{app.js,app.css,index.html}`/`app.spec`/`check_worker_lifecycle`/`checks.yml`); `docs/planning/` NOT staged; **not pushed** (`origin/main` still `d2ee353`). Final verification: **56/56** Python suite + Node renderer + byte-compile + `node --check` + diff-check green at commit time. Still owed: branch-CI run of the gates + work-PC acceptance (§M; not DoD). **Next eligible phase: P2** (`pending`; depends on P1 ✓). |
| **P2** | `committed` (Codex round 7 `PASS`) | `e47b700` | [`phases/P2-claude-report.md`](phases/P2-claude-report.md) | Transactional artifact lifecycle (F2/F9/F5). Baseline `e47b700` (P1 committed); branch `refactor/v0.18.0-structural-overhaul`. **Verification green (offline):** new `scripts/artifact_store.py` — atomic single-file write (`atomic_save`), the multi-file `commit_workbook` wrapper (values-canonical/formulas-best-effort, validate-before-commit, temp-name rewrite; compare_core untouched), input `fingerprint`/`consolidated_fresh`, and journaled `promote_store`/`recover_promotions`. **F2** `gui_worker._swap_store_dir` → delegates to the journaled promotion; `updater.cleanup_leftovers` runs the recovery sweep on every launch. **F9** the base + 3 standalone consolidators + 4 `tsn_load_*` writers use `atomic_save`; the matrix vs-TSN/self/cross-env comparison writes + `_try_formulas` + the Compare-tab `mode="both"` call route through `commit_workbook`. **F5** `matrix._consolidated_stale` is fingerprint-based (a deleted route no longer reads fresh); per-cell `input_fingerprint` in the matrix/by-day records makes `_cmp_state`/`comparison_state` read a cell stale on input-identity change; `day_matrix` day-level fixes the `all(... if exists)` gap. **R1-R15/RR3-C3** `cache_envelope.SCHEMA_VERSION` 1→2 (single released value; one rebuild) + an `input_fingerprint` slot. `app.spec` APP_MODULES += `artifact_store` (F6). **New CTs:** `check_artifact_store.py` (CT-4/5/8/8b + fingerprint) + `check_p2_freshness.py` (15 — CT-6a/b/c, CT-7), each proven RED on pre-fix code via revert/run/restore. **56/56** Python suite (54+2) + Node renderer + byte-compile + `node --check app.js` + `check_import_direction` + `check_app_modules` + YAML + `git diff --check` all green. No UI changes; `compare_core`/updater-TLS/auth untouched. **Codex round 1 = `BLOCKED`** (5 blocking P2-B01..B05 + 3 required P2-R01..R03 + 2 recs, all real) → **all 5+3 resolved + both recs applied (round-1 remediation):** **P2-B01** `_run_specs` captures the promotion bool → a failed swap reads `previous_preserved` (no auto-consolidate; `BatchWorker` env-done now gates on `artifact==PROMOTED`); **P2-B02** `promote_store`/`_recover_one` RETAIN the journal+backup on a failed restore (deleted only after a canonical copy exists) + surviving-staging last resort; **P2-B03** `updater` recovery sweeps `OUTPUT_ROOT` **and** `settings.get_batch_dest()` (deduped, isolated); **P2-B04** `_trusted_journal_names` rejects non-basename/`..`/absolute/wrong-token records (touch nothing) + orphan sweep dir-only; **P2-B05** `_openable_xlsx` actually OPENS via openpyxl + checks `expect_sheet="Comparison"` (rejects a malformed XLSX-shaped ZIP, prior kept); **P2-R01** staging needs a real report FILE; **P2-R02** error/cancelled results sanitized (full-path **+** basename map, `message` included); **P2-R03** formulas-commit failure returns a truthful values-canonical result; **P2-A01** stale newest-mtime docstrings corrected; **P2-A02** before/after fingerprint guard (`built_from`). **New regression tests proven RED on pre-fix code (revert/run/restore):** `check_artifact_store` (67→**106**) covers B02/B03/B04/B05/R01/R02/R03/A02; `check_batch_outcome` (24→**29**) the B01 failed-promotion (stub now RETURNS the bool, per Codex). **56/56** Python suite + Node renderer + byte-compile + `node --check` + import-direction + app_modules + YAML + diff-check green; no `REVERT-PROOF` marker remains; `compare_core`/updater-TLS/auth/UI untouched. Round-1 remediation appended to the report. **Codex round 2 = `BLOCKED`** (round-1 fixes confirmed; 3 blocking P2-B02/B04/B06 + 2 required P2-R01/R04 + 3 recs, all real) → **all 3+2 resolved + 3 recs applied (round-2 remediation):** **P2-B02** recovery requires `target` be a USABLE store (has a report) before deleting any copy — an empty placeholder no longer lets it delete the valid backup; restores from backup/staging, retains on a foreign-content conflict; **P2-B04** the journal-free orphan sweep REMOVED — `recover_promotions` touches only trust-validated journal paths (an unrelated `*.bak-*`/`*.staging` under a user dest survives); **P2-B06** the FIRST promotion is journaled too (failed rename retains staging+journal → recovery promotes it; never deletes the only copy) + `_run_specs` reads `artifact=none` (not `previous_preserved`) when no prior existed; **P2-R01** staging must hold a real report artifact (`.xlsx`/`.pdf`), not any file; **P2-R04** `_openable_xlsx` opens on a `with`-managed handle so a malformed workbook leaves no locked temp; **P2-A01/A02/A03** docstrings corrected, race removes the stale sidecar, `_try_formulas` logs a non-ok commit. **New regression tests proven RED on pre-fix code (revert/run/restore):** `check_artifact_store` (106→**129**) covers B02/B04/B06/R01/R04/A02; `check_batch_outcome` (29→**30**) the failed-first-promotion `none`. **56/56** Python suite + Node renderer + byte-compile + `node --check` + import-direction + app_modules + YAML + diff-check green; no `REVERT-PROOF` marker remains; `compare_core`/updater-TLS/auth/UI untouched. Round-2 remediation appended to the report. **Codex round 3 = `BLOCKED`** (rounds 1–2 fixes confirmed; P2-B04 narrowed to ownership + P2-R05 new + 3 recs) → **both resolved + 3 recs applied (round-3 remediation):** **P2-B04** `recover_promotions(root, is_owned)` takes a caller-supplied ownership predicate (updater builds it from `<src>-<env>` store roots + `EXPORT_REPORTS` subdirs); a shape-valid but UNOWNED `.promote` tree under a user dest is untouched; **P2-R05** recovery removes the journal only after every residue is CONFIRMED gone (`_rmtree_gone`) — a locked backup/staging RETAINS the journal for retry; **P2-A02** an un-removable stale sidecar is overwritten with a non-matching sentinel (`_write_fp_sentinel`); **P2-A04** `had_prior` uses `is_usable_store` (a failed first promotion over an empty/foreign dir reads `none`); **P2-A01** duplicated comment removed. **New regression tests proven RED on pre-fix code (revert/run/restore):** `check_artifact_store` (129→**143**) covers B04-unowned/R05/A02-unremovable; `check_batch_outcome` (30→**31**) the empty-prior `none`. **56/56** Python suite + Node renderer + byte-compile + `node --check` + import-direction + app_modules + YAML + diff-check green; no `REVERT-PROOF` marker remains; `compare_core`/updater-TLS/auth/UI untouched. Round-3 remediation appended to the report. **Codex round 4 = `BLOCKED`** (rounds 1–3 fixes confirmed; P2-B04 ownership still basename- not location-based + P2-R05 normal promote path + P2-A02 dual-failure rec) → **both resolved + rec applied (round-4 remediation):** **P2-B04** ownership is now LOCATION-based and established BEFORE any journal read/delete — `recover_promotions` runs a `is_owned(store_root, None)` location gate per `.promote` (updater requires `store_root.parent.resolve()==recovery_root` direct-child + known `<src>-<env>` name), so a nested valid-NAME-wrong-LOCATION tree (and its malformed journals) is untouched; **P2-R05** a shared `_finalize_journal` drops the journal only after every residue is confirmed gone — applied to BOTH `promote_store` completion branches (promotion-success backup, inline-restore staging) so a locked residue retains the journal for retry; **P2-A02** the race ladder gains a final `_quarantine_workbook` rung (both sidecar ops fail → rename the replaced workbook aside → reads stale). **New regression tests proven RED on pre-fix code (revert/run/restore):** `check_artifact_store` (143→**156**) — B04 nested-location/malformed + R05 promote-path (locked backup/staging) + A02 dual-failure quarantine. **56/56** Python suite + Node renderer + byte-compile + `node --check` + import-direction + app_modules + YAML + diff-check green; no `REVERT-PROOF` marker remains; `compare_core`/updater-TLS/auth/UI untouched. Round-4 remediation appended to the report. **Codex round 5 = `BLOCKED`** (rounds 1–4 fixes confirmed; one new P2-B07 exposed by the round-4 journal retention) → **resolved (round-5 remediation):** **P2-B07** two same-target journals could let recovery roll back past last-good — each journal now records a `seq` generation marker (`time.time_ns()`) and `recover_promotions` processes a target's journals NEWEST-`seq`-FIRST (stable name tie-break, filesystem-order-independent), so the newer generation restores/keeps the true last-good (V2) before any older journal can restore an older generation (V1) over it or delete the newer backup/staging (Codex's generation-aware-recovery option). **New regression proven RED on pre-fix code (revert/run/restore):** `check_artifact_store` (156→**164**) — `test_b07_two_journals_generation_aware` seeds the reachable two-generation interrupted state (V1 older-journal backup, V2 newer-journal backup, V3 shared staging, live absent) and asserts recovery keeps V2 + discards V1/V3 in BOTH name orderings. **56/56** Python suite + Node renderer + byte-compile + `node --check` + import-direction + app_modules + YAML + diff-check green; no `REVERT-PROOF` marker remains; `compare_core`/updater-TLS/auth/UI untouched. Round-5 remediation appended to the report. **Codex round 6 = `BLOCKED`** (rounds 1–5 fixes confirmed; P2-B07 re-opened — the round-5 `seq` generation was wall-clock-based, which can regress across restarts/clock corrections) → **resolved (round-6 remediation):** **P2-B07** the generation marker is now a DURABLE monotonic counter — `_next_generation(jdir, target)` = `max(existing same-target journal gen)+1` (derived from on-disk journals under the single-writer boundary, NOT wall-clock); `promote_store` stamps `"gen"` (the `"seq"`/`time` is gone), `recover_promotions` orders by `_journal_gen` desc — so a later promotion's gen is strictly greater than every existing same-target journal regardless of the clock, and an older journal can never out-rank a newer one even after a clock rollback. **New regression proven RED on pre-fix code (revert/run/restore):** `check_artifact_store` (164→**169**) — `test_b07_durable_generation` asserts `_next_generation` is `max+1` and that two REAL same-target promotions yield strictly increasing gens `[1,2]` (a counter, not a timestamp); the two-journal recovery test now uses the durable `gen` in both filename orders. **56/56** Python suite + Node renderer + byte-compile + `node --check` + import-direction + app_modules + YAML + diff-check green; no `REVERT-PROOF` marker remains; no `seq`/`time` survives in artifact_store; `compare_core`/updater-TLS/auth/UI untouched. Round-6 remediation appended to the report. **Codex round 7 = `PASS`** (no blocking/required findings; P2-B01..B07, P2-R01..R05, P2-A01..A04 all Resolved — P2-B07 closed via the durable `_next_generation` strictly-increasing same-target generation; Codex verified no wall-clock `seq` remains, no `REVERT-PROOF` marker, `compare_core`/frontend/auth/updater-TLS outside the diff). **Committed `ca3c2af`** "feat: add transactional artifact lifecycle (F2/F9/F5)" — the **23** intended product files staged by explicit path (**3 new:** `artifact_store` + `build/check_artifact_store` + `build/check_p2_freshness`; **16 modified product:** `cache_envelope`/`matrix`/`day_matrix`/`gui_worker`/`gui_api`/`updater`/`consolidate_xlsx_base`/`consolidate_intersection_summary`/`consolidate_ramp_summary`/`consolidate_tsn_highway_sequence`/4 `tsn_load_*`/`app.spec`/`checks.yml`; **4 modified tests:** `check_consolidate_outcome`/`check_tsn_outcome`/`check_batch_outcome`/`check_worker_lifecycle`). NB the report §4 "Files affected" lists **2** modified tests — it predates the remediation rounds that also updated `check_batch_outcome` (B01/B06/A04 promotion-bool stubs) and `check_worker_lifecycle` (success stub stamps `artifact=PROMOTED`) for the P2-B01 env-done `artifact==PROMOTED` gate; both diffs are P2-only, no assertion weakened. `docs/planning/` NOT staged; **not pushed** (`origin/main` still `d2ee353`). Final verification at commit: **56/56** offline Python suite + Node renderer + byte-compile + `node --check app.js` + `git diff --check` clean; `check_artifact_store` 169 assertions; no `REVERT-PROOF` marker; `compare_core`/updater-TLS/auth/UI untouched. Still owed: branch-CI run of the gates + work-PC live promotion-recovery + one-time rebuild (§M; not DoD). **Next eligible phase: P3** (`pending`; depends on P0 ✓). |
| **PA** | `committed` (Codex round 3 `PASS`) | `4bbee65` | [`phases/PA-claude-report.md`](phases/PA-claude-report.md) | Exact-artifact packaging safety gate. **Verification green (offline):** F6 reproduced→closed (`check_app_modules` red→green; `APP_MODULES` 51→55); `gui_main --self-test` runs end-to-end in dev (EXIT 0 — browser+pdf+download, pdfplumber, openpyxl, F6 trio, GUI bridge, hidden WebView2, `TSMIS_SELFTEST_OUT`); `full_smoke.py` delegates cleanly (EXIT 0); **50/50** suite + byte-compile + `build.ps1` parse + 3 workflows valid YAML + `check_no_misspelling` + `check_import_direction` all pass; `git diff --check` clean. Deliverables: `gui_main --self-test` + shared `scripts/self_test.py` (extracted from `full_smoke`, D24); `app.spec` += F6 trio (`matrix`/`day_matrix`/`report_library`) + `self_test` + UI `datas` extension allowlist; new offline checks `build/check_app_modules.py` + `build/check_source_zip_smoke.py`; `build.ps1` docs-before-prune (F10) + `-SelfTest` gates the exact windowed exe; CI wiring (checks.yml +2; new `frozen-gate.yml` label/nightly; `release.yml` ships the gated artifact). **Codex round 1 = `BLOCKED`** (PA-B01 no frozen-build evidence; PA-B02 frozen-gate stale after label; PA-B03 source-zip gate tested the checkout not an archive; PA-A01 tempdir leak) → **all resolved (round-1 remediation):** **PA-B01** — ran the local **disposable** frozen builds: **both** exact windowed variants built + their exact `TSMIS Exporter.exe --self-test` **PASSED** (system **153 MB**, with-browser **527 MB**; F6 trio imported in the frozen bundle; F10 docs present + DLP-scanned; UI allowlist shipped only the 3 web assets). **PA-B02** — `frozen-gate.yml` triggers += `synchronize`/`opened`/`reopened`, job `if` conditions on the PR's current label set. **PA-B03** — `check_source_zip_smoke.py` rewritten to gate a **real `git archive` extract** in a fresh interpreter (membership + prefix + clean-root + **negative** missing-member test). **PA-A01** — `self_test.run` try/finally (cleanup on failure verified). Re-verified: **50/50** suite + byte-compile + archive gate (incl. negative) + YAML all green; `dist/`+`pyi-work` reclaimed (git-ignored throughout). **Codex round 2 = `BLOCKED`** (narrowed): PA-B01/B02/A01 confirmed Resolved; **PA-B03 still open** — the archive gate ran `git archive HEAD`, which (PA uncommitted per D20) tested **P0**, not the PA revision. **Round-2 remediation:** `check_source_zip_smoke.py` now defaults to a **worktree-candidate** archive built via a **throwaway git index** (real index untouched; tracked+untracked product files, `.gitignore` respected, `docs/planning/` excluded) → gates the **uncommitted PA revision**; new `--zip` mode + `release.yml` gates the **exact uploaded** zip; added a worktree-provenance assertion. Demonstrated: candidate archive contains the 4 PA additions + 0 `docs/planning`; an injected uncommitted break FAILS the gate then restores green; index provably untouched; `--zip` mode green. Corrected the round-1 claim (that run tested HEAD/P0, not PA). **50/50** + YAML + diff-check re-green. Round-1 & round-2 remediation appended to the report. **Codex round 3 = `PASS`** (no blockers/required fixes). **Committed `65aef98`** "chore: add exact-artifact packaging gate (--self-test, reachability, source-zip)" — the 10 intended product files staged by explicit path; `docs/planning/` not staged; **not pushed** (`origin/main` still `d2ee353`). Still owed: branch-CI run of the same gates + work-PC acceptance (§M; not DoD). **Next eligible phase: P1** (`pending`; depends on P0 ✓). See D24. |
| **P3** | `committed` (Codex round 3 `PASS`) | `ca3c2af` | [`phases/P3-claude-report.md`](phases/P3-claude-report.md) | Stable-ID taxonomy + manifest v1/v2 migration (F7). Baseline `ca3c2af` (P2 committed); branch `refactor/v0.18.0-structural-overhaul`. **Verification green (offline):** `reports.py` gains the 4-tier stable IDs (§C.5) — `EXPORT_KEYS` (derived = subdirs, 7), `CONSOLIDATE_KEYS` (8, HL split `cons:highway_log_excel/pdf/tsn_highway_log`), `COMPARE_KEYS` (15, composite `cmp:<family>:<flavor>`) + lookups (`spec_for_export_key`/`resolve_export_keys` order-preserving+dedup+reject-with-log, `consolidate_index_for_key`, `compare_index_for_key`) + **import-time uniqueness/length asserts**; matrix `row_key` NOT renamed. **`batch_manifest` v1→v2** persists export-op KEYS via a **frozen `_V017_EXPORT_ORDER`** index→key map; `load()` accepts v1 AND v2, normalizes to v2 keys in memory (out-of-range/non-string dropped+logged, deduped) + stamps version=2 so the next `save` rewrites forward exactly once; unsupported version → None. **`gui_worker._specs`** resolves manifest KEYS→specs (unknown/disabled dropped+logged, never mis-resolved by position) + **no-env-done-on-empty** guard (a batch whose saved reports all vanished can't false-complete). **`gui_api`** start_export/start_batch_export take `report_keys` (canonical keys persisted); the 7 consolidate/compare endpoints take `report_key` via `*_index_for_key`; initial state carries `key`. **`app.js`** selection by `dataset.key` (+ `reportByKey`, env-scan overlays read the row key, batch-library `startBatch([subdir])`) + the FULL `#mock` key wiring (`repByKey`/`consByKey`). **New `build/check_stable_ids.py`** (CT-9 + R1-T05, 24 assertions: uniqueness/round-trip, resolve dedup/reject, v1→v2 migration, the **F7 re-order proof** [saved keys still resolve to the same reports after the registry list is reversed], CT-9 resume resolution, no-env-done-on-empty) — the empty-resolution guard proven **RED via revert/run/restore** (3 assertions fail without it). 5 index-coupled tests migrated to keys (`check_b3_batch` +v1→v2/reject coverage, `check_gui_bridge`, `check_matrix_bridge`, `check_a2_compare_filter`, `check_intersection_gate`) — no assertion weakened. 2 canonical docs ("selection is by index") corrected (broader reconciliation stays P11). **57/57** Python suite (56→57) + Node renderer + byte-compile + `node --check` + import-direction + app_modules + product-name guard + YAML + diff-check green; **`#mock` browser-verified** — 7 export + 8 consolidate + 15 compare rows ALL carry `dataset.key`, full export/batch/consolidate run by KEY with **0 console errors**, summary labels resolve via `repByKey`. `compare_core`/updater-TLS/auth/`app.spec`/`version.py`/matrix `row_key` untouched. **Owed:** work-PC resume of a REAL v0.17 paused batch (live on-disk v1→v2 migration; §M; not DoD). **Next eligible phase: P4** (`pending`; needs P3 committed + PA ✓; P6/P7a/P8a are already eligible independently of P3). **Codex round 1 = `BLOCKED`** (3 blocking P3-B01/B02/B03, all verified real) → **all 3 resolved (round-1 remediation):** **P3-B01** manifest + selection validation is now ALL-OR-NOTHING — `batch_manifest._migrate_v1_reports`/`_normalize_reports` are 1:1 length-preserving (bool/float/str-v1 + out-of-range + non-string-v2 → poison `_INVALID_KEY`, NO coercion/drop, dups KEPT), `build` drops `str()`, `reports.resolve_export_keys` → `(specs, invalid)` rejecting **duplicates** + unknown/disabled, and `start_export`/`start_batch_export`/`BatchWorker.run` reject the WHOLE selection on any invalid (abort, **manifest preserved, no env done**) — closing Codex's `["ramp_summary","__removed__"]` false-complete repro; **P3-B02** the invalid/empty abort emits exactly ONE terminal (`error`, mirroring the AuthError batch path) — no `batch_done`; CT-10 gains a real `_batch_invalid` producer scenario + a successor-integrity test (single terminal advances a queued successor; `mark_done` stubbed to raise); **P3-B03** `compareKind()` resolves by key (`currentCompareRep().kind`) instead of array-indexing the `cmp:*` string (which defaulted every compare to "files", mis-routing folder compares to `start_compare`) — **new `build/check_compare_routing.js`** (Node vm) locks folders→`start_compare_env`/files→`start_compare`, proven **RED** on the pre-fix code + **re-verified live in `#mock`** (folder/file section toggle + routing, 0 console errors). **57/57** Python suite + **2** Node frontend checks (`mx_partial`+`compare_routing`) + byte-compile + `node --check` + import-direction + app_modules + no-misspelling + YAML + diff-check green; no `REVERT-PROOF` marker. Remediation touched `reports`/`batch_manifest`/`gui_worker`/`gui_api`/`app.js` + `check_stable_ids`/`check_b3_batch`/`check_worker_lifecycle` + new `check_compare_routing.js` + `checks.yml`; `compare_core`/auth/updater-TLS/`app.spec`/`version.py`/matrix `row_key` untouched. Round-1 remediation appended to the report; phase kept `awaiting_review`. **Codex round 2 = `PASS WITH FIXES`** (no blockers — P3-B01/B02/B03 confirmed Resolved; one required **P3-R01** doc cleanup) → **resolved (round-2 remediation):** **P3-R01** the eight P3-invalidated index-contract statements now describe the stable-KEY contract — `reports.py` (COMPARE tail comment + `enabled_export_reports`/`export_reports_status` docstrings: `idx` = display/current-order metadata, manifests/start_export travel by key, a disabled report is rejected by its KEY server-side), `ui/app.js` (the 2 mock CONS/compare comments: radios carry `cons:*`/`cmp:*` keys), `check_stable_ids.py` docstring (v1 migrates 1:1/poison/**keeps duplicates**, not de-duplicated), `docs/reports.md` + `docs/verification-and-testing.md` (disabled rejected by key; GUI/manifests pass stable keys). Documentation/comment-only — no product behavior / test logic / CI change; narrow per Codex (no P11 broadening). **57/57** Python suite + 2 Node frontend checks + byte-compile + `node --check` + no-misspelling + diff-check green; residual P3-invalidated-wording grep across the 5 cited files = none; no `REVERT-PROOF` marker. Round-2 remediation appended to the report; phase kept `awaiting_review`. **Codex round 3 = `PASS`** (no blocking/required findings; P3-B01/B02/B03 + P3-R01 all Resolved). **Committed `5defe9e`** "feat: add stable-ID taxonomy + manifest v1/v2 migration (F7)" — the **17** intended product files staged by explicit path (**5 product:** `reports`/`batch_manifest`/`gui_worker`/`gui_api`/`ui/app.js`; **2 new tests:** `check_stable_ids` + `check_compare_routing.js`; **6 modified tests:** `check_a2_compare_filter`/`check_b3_batch`/`check_gui_bridge`/`check_intersection_gate`/`check_matrix_bridge`/`check_worker_lifecycle`; **1 CI:** `checks.yml`; **3 docs:** `architecture`/`reports`/`verification-and-testing`); `docs/planning/` NOT staged; **not pushed** (`origin/main` is `180c6e4` — the remote advanced since the P2 record; the feature branch is 7 commits ahead, unpushed). Final verification at commit: **57/57** offline Python suite + 2 Node frontend checks (`mx_partial` + `compare_routing`) + byte-compile + `node --check app.js` + import-direction + app_modules + no-misspelling + `git diff --check` clean; no `REVERT-PROOF` marker; `compare_core`/auth/updater-TLS/`app.spec`/`version.py`/matrix `row_key` untouched. Still owed: work-PC resume of a real v0.17 paused batch (live on-disk v1→v2; §M; not DoD). **Next eligible phase: P4** (`pending`; depends P3 ✓ + PA ✓; P6/P7a/P8a also eligible independently of P3). |
| **P4** | `committed` (Codex round 5 `PASS`) | `5defe9e` | [`phases/P4-claude-report.md`](phases/P4-claude-report.md) | Report metadata catalog (narrowed) + `.bat` fix + parity (F6 / R1-B05/M01/D02/D05/R13/T05). Baseline `5defe9e` (P3 committed); branch `refactor/v0.18.0-structural-overhaul`. **Verification green (offline):** new **`scripts/report_catalog.py`** = the report-metadata SoT (R1-B05: metadata + module refs ONLY — packaging reachability [`check_app_modules`] + test oracles [fake-site, the golden baseline] stay independent) holding the ordered EXPORT(7)/CONSOLIDATE(8)/COMPARE(15) op descriptors + keys + `COMPARE_GROUPS` + the auto-consolidator-by-subdir map + the 6 TSN descriptors (lazy `module:function` builder strings); **`reports.py` now DERIVES** every registry name from it (the logic — disable gate, `matrix_rows`, P3 stable-ID lookups, integrity asserts — stays in `reports.py`; public API byte-for-byte unchanged); **`tsn_library._REPORTS` DERIVED** from `catalog.tsn_entries()`. The catalog import-hub is **cycle-safe** (no `export_*`/`consolidate_*`/`compare_*` leaf imports `reports`/`tsn_library`/`report_catalog`; `check_import_direction` green). **R1-M01 `.bat` fix:** the two missing **Intersection** consolidators added to `4. consolidate (combine reports).bat` (6→8 options, registry-ordered). `app.spec` APP_MODULES += `report_catalog` (F6 completeness). **New `build/check_report_catalog.py`** (24 assertions): **DERIVE** (reports/tsn_library == the catalog views), **GOLDEN EQUIVALENCE** (the catalog metadata + keys + TSN descriptors == a FROZEN v0.17 baseline literal — an INDEPENDENT approved snapshot, so behavior-neutral, R1-R13), **.bat PARITY** (the console menu covers exactly the registry consolidators, R1-M01), **DYNAMIC-IMPORT RESOLVABILITY** (every lazy TSN builder module resolves, R1-T05), **MOCK PARITY** (`#mock` export/cons/compare key lists == catalog keys in order, R1-R13/CT-13). RED proofs (revert/run/restore): the `.bat` parity → 3 fails on a planted drift; the golden guard → fails on a catalog-label drift while DERIVE still passes (proving it is the independent drift guard). Docs: the P4-invalidated SoT / "`.bat` hand-edited" statements corrected narrowly (`docs/architecture.md`, `docs/reports.md`, `CLAUDE.md`; broader reconciliation stays P11). **58/58** Python suite (57→58) + 2 Node frontend checks (`mx_partial` + `compare_routing`) + byte-compile + `node --check app.js` + import-direction + app_modules + no-misspelling + diff-check green; no `REVERT-PROOF` marker. `compare_core`/auth/updater-TLS/`version.py`/`app.js`/matrix `row_key`/the independent fake-site oracle all untouched. **Owed:** none beyond the standard branch-CI run (pure offline refactor — no work-PC dependency). **Next eligible phases (unlocked by P4):** P5 (conditional/deferrable; depends P3, P4), and the now-eligible **P9** (mock separation; needs P4, PA) + **P10** (packaging; needs PA, P4) — with **P6/P7a/P8a** still eligible independently. **Codex round 1 = `PASS WITH FIXES`** (no blockers — no product defect; 4 required: the checks proved less than claimed + false dependency-boundary claims) → **all 4 resolved (round-1 remediation):** **P4-R01** golden equivalence now pins each export key's exact `ReportSpec`, each comparison key's adapter, and each consolidate + auto-consolidator module by **object identity** against an INDEPENDENT oracle the check imports itself; TSN builders resolve via `import_module`+`getattr`+`callable`; + 4 negative self-tests (wrong adapter/auto-consolidator/spec/missing-fn each fail) — RED-proven (re-pointing COMPARE[0]'s adapter fails GOLDEN while DERIVE still passes — Codex's exact diagnostic, now caught); **P4-R02** `.bat` parity now asserts the **ordered** `choice→goto→block→module` chain == registry order (+ uniqueness/reachability) with a negative swapped-dispatch case; **P4-R03** mock parity now compares the `#mock` lists to the **REAL bridge** (`get_initial_state`) **field-for-field** (label/format/kind/group/id), not keys, + the report's CT-13/P9 over-claim corrected; **P4-R04** removed the false "never pulls pdfplumber/openpyxl" / "import-light" claims from `report_catalog`/`reports`/`tsn_library`/the check/`architecture.md`/the phase report (each now: console-free but NOT dependency-light — `import report_catalog` loads 705 modules incl. openpyxl/pdfplumber), + fixed the 3 stale `reports.md` lines (CompareEntry/TsnEntry recipes; Intersection Summary registered). `check_report_catalog` **24→37** assertions; ASCII-clean output (passes on cp1252 too). **No product behavior changed** (catalog data byte-identical; only the CHECK got stronger + the CLAIMS truthful). **58/58** Python suite + 2 Node frontend checks + byte-compile + `node --check` + import-direction + app_modules + no-misspelling + diff-check green; no `REVERT-PROOF` marker; residual false-claim sweep clean; `compare_core`/auth/updater-TLS/`version.py`/`app.js`/matrix `row_key` untouched. Round-1 remediation appended to the report; phase kept `awaiting_review`. **Codex round 2 = `PASS WITH FIXES`** (no blockers; R01–R04 partially resolved + new **P4-R05**) → **all 5 resolved (round-2 remediation):** **P4-R01** the negative self-tests now run **mutated catalog inputs through the production identity helpers** (wrong adapter/spec/module/auto-consolidator/missing-fn each rejected), not "known objects differ"; **P4-R02** `.bat` parse is now ordered + label-aware + **no-dict-collapse** (exactly-one dispatch, unique blocks, displayed-label-startswith, FIRST-match chain) + negatives for wrong label / duplicate dispatch / duplicate block / swapped goto; **P4-R03** mock parity is now **FIELD-FOR-FIELD** vs the pure bridge payload (export key/idx/label/fmt/disabled, cons key/label/fmt, groups id/label, compare key/label/kind/group/file_a/file_b) + the separate `CONS_REPORTS` routing list == registry + the bridge `subdir` cross-checked (subdir is bridge-only — the frontend resolves folders server-side, not via `compare_reports[].subdir`); **P4-R04** the 3 missed claims fixed (`gui_api`:2185/2579 "(no pdfplumber pull)" + `architecture.md`:352-353 — lazy builder *invocation* ≠ eager dependency import); **P4-R05** extracted a PURE **`gui_api._report_list_payload()`** (no `GuiApi` → no `ensure_layout` writes, no thread starts), `get_initial_state` spreads it (behavior-neutral — `check_gui_bridge`/`check_intersection_gate` green), the check uses it directly, + a `test_no_side_effects` guard proves the parity path makes 0 `ensure_layout`/thread/`GuiApi` calls. `check_report_catalog` **37→47** assertions; ASCII-clean. **No product behavior changed** (catalog byte-identical; same payload). **58/58** Python suite + 2 Node frontend checks + byte-compile + `node --check` + import-direction + app_modules + no-misspelling + diff-check green; no `REVERT-PROOF`; residual "(no pdfplumber pull)" sweep clean; `compare_core`/auth/updater-TLS/`version.py`/`app.js`/matrix `row_key` untouched. Round-2 remediation added `gui_api.py` to the changeset (pure-payload extraction). Round-2 remediation appended to the report; phase kept `awaiting_review`. **Codex round 3 = `PASS WITH FIXES`** (no blockers; **P4-R01/R03/R04/R05 RESOLVED**; only **P4-R02** still open, narrowed) → **resolved (round-3 remediation):** the `.bat` parser recognized a block only when a `:label` was IMMEDIATELY followed by the python line, so a RAW duplicate `:intersection_summary` with an intervening `rem` before a WRONG `consolidate_intersection_detail.py` call (which CMD's `goto` actually runs — it jumps to the FIRST label) stayed invisible — RED-confirmed (old matched-block count 1, `_blocks_exact`=True, chain == registry). **Fix** (`build/check_report_catalog.py`, `.bat` section only — NO product change): `_parse_bat` rewritten as a line-scanner enumerating **every** raw `:label` + its body up to the NEXT label (`blocks=[(label,[modules])]`); new `_targets_defined_once` requires each dispatched goto-target defined by exactly one raw label; `_bat_chain` resolves choice->FIRST goto->FIRST block->the block's **sole** consolidator (0/2+ invocations -> invalid); + a new negative that injects the decoy into the real `.bat` text and re-parses, asserting BOTH uniqueness-rejection AND wrong-FIRST-match-chain (existing wrong-label / dup-dispatch / immediate-dup-block / swapped-goto negatives preserved). NEW parser RED-proven to catch the decoy (2 raw labels, `targets_defined_once`=False, chain!=registry, choice-4 FIRST block -> `consolidate_intersection_detail`). `check_report_catalog` **47->49** assertions; ASCII-clean. **No product behavior changed** (`app.js`/`gui_api.py`/`report_catalog.py`/matrix `row_key` untouched this round). **58/58** Python suite + 2 Node frontend checks + byte-compile + `node --check` + import-direction + app_modules + no-misspelling + diff-check green; no `REVERT-PROOF`. Round-3 remediation appended to the report; phase kept `awaiting_review`. **Codex round 4 = `PASS WITH FIXES`** (no blockers; **P4-R01/R03/R04/R05 remain RESOLVED**; only **P4-R02** still open, narrowed to **block termination**) → **resolved (round-4 remediation):** the `.bat` parser recorded only consolidator names per block and required no terminating transfer, so removing ONLY `:intersection_summary`'s `exit /b 0` left all 5 main conditions green while CMD falls through `:intersection_detail` and runs Detail too — RED-confirmed (old parser kept all 5 green, modeled no termination). **Fix** (`build/check_report_catalog.py`, `.bat` section only — NO product change): `_parse_bat` now records per block an **ordered event list** (`("cons",module)` / `("term",line)`); module-level `_TERMINATOR` accepts UNCONDITIONAL `exit /b [code]` / `exit [code]` / `goto :eof` (rejects `if ...`-prefixed); new `_terminates_after_consolidator` requires exactly one consolidator THEN a terminator before the next label, `_all_blocks_terminate` requires it of every dispatched block; new production assertion + a negative that removes the real `exit /b 0`, asserting BOTH the termination guard rejects it AND the module chain stays green (distinct guard); duplicate-block negative migrated to the event shape; all round 2-3 negatives preserved. NEW guard RED-proven (real file all-terminate=True; `exit /b 0`/`exit /b`/`exit 1`/`goto :eof` accepted, `if errorlevel 1 exit /b 1` rejected; removed terminator -> `_all_blocks_terminate`=False while chain==registry). `check_report_catalog` **49->52** assertions; ASCII-clean. **No product behavior changed** (`app.js`/`gui_api.py`/`report_catalog.py`/matrix `row_key` untouched this round). **58/58** Python suite + 2 Node frontend checks + byte-compile + `node --check` + import-direction + app_modules + no-misspelling + diff-check green; no `REVERT-PROOF`. Round-4 remediation appended to the report; phase kept `awaiting_review`. **Codex round 5 = `PASS`** (no blocking/required findings; P4-R01..R05 all Resolved). **Committed `c86dc78`** "refactor: add report-metadata catalog + console-menu parity (F6)" — the **11** intended product files staged by explicit path (**2 new:** `scripts/report_catalog.py` + `build/check_report_catalog.py`; **9 modified:** `reports.py`/`tsn_library.py`/`gui_api.py`/`app.spec`/`4. consolidate (combine reports).bat`/`checks.yml`/`architecture.md`/`reports.md`/`CLAUDE.md`); `docs/planning/` NOT staged; **not pushed**. Final verification at commit: **58/58** offline Python suite + `check_report_catalog` (52 assertions) + 2 Node frontend checks (`mx_partial` + `compare_routing`) + byte-compile + `node --check app.js` + import-direction + app_modules + no-misspelling + `git diff --check` clean; no `REVERT-PROOF` marker; `compare_core`/auth/updater-TLS/`version.py`/`app.js`/matrix `row_key` untouched. **Owed:** none beyond the standard branch-CI run (pure offline refactor — no work-PC dependency). **Next eligible phase: P5** (`pending`; depends P3 ✓ + P4 ✓; conditional/deferrable) — with **P6/P7a/P8a** (independent, already eligible) and the now-unlocked **P9/P10** (need P4 ✓ + PA ✓) also eligible. |
| **P5** | `committed` (Codex round 3 `PASS`) | `c86dc78` | [`phases/P5-claude-report.md`](phases/P5-claude-report.md) | Report-family DRY — **family 1: the `tsn_load_*` single-file TSN normalizers** (S04/R1-N01). Baseline `c86dc78` (P4 committed); branch `refactor/v0.18.0-structural-overhaul`. **Conditional/deferrable phase, user-selected for this cycle.** The four `tsn_load_*` loaders shared one ~30-line skeleton (find newest raw → deps gate → overwrite-confirm → parse → write a styled write-only workbook → atomic save → PermissionError guard) duplicated 4×; P5 collapses it into **one `tsn_library.build_normalized` factory** (+ `_newest_raw` + `_write_normalized_workbook`) and reduces the four modules to **thin shims** — each keeps a `from openpyxl import Workbook` deps probe, `RAW_GLOB`, a `_project(raw_path) → (rows, make_result)` that calls its `compare_*_tsn` projection (`tsn_rows_from_raw`/`parse_tsn_pdf`), and a `build_into` delegating to the factory. **`build_into` contract + the `report_catalog` lazy builder strings (`tsn_load_*:build_into`) preserved.** **Behavior-neutral, PROVEN:** an old-vs-new harness (`git show c86dc78:` each old module loaded beside the new one) ran both `build_into` on identical synthetic input for all 4 loaders (summaries full + partial) and compared the full `ConsolidateResult` (status/message/summary_lines/completion/skipped_inputs) AND the produced workbook (sheet/header values/per-cell alignment+font+fill/data rows) → **6/6 semantically identical** (workbook semantics + result contract, not XLSX ZIP bytes), incl. the 3 distinct header-alignment variants (detail center/center/wrap, ramp-summary center/center, intersection-summary center-only). Producer completion (P1-B05) preserved — summaries still flag PARTIAL+`skipped_inputs` on a missing category; details set none (inferred complete from `status="ok"`). **New `build/check_tsn_normalizer.py`** (**28** assertions after round-1 remediation, ASCII-clean, **RED-proven**) locks the factory against an **independent frozen oracle**: per-case every `ConsolidateResult` field + exact event-log line + sheet/header/data rows + header alignment/font/fill, the exact shared error/cancel strings + branches (deps gate / missing-raw / overwrite-confirm / parse-error / newest-by-mtime / `~$`-lock skip), the atomic-save PermissionError contract, and the P5-A01 backstop; wired blocking into `checks.yml` after `check_tsn_outcome`. **Cycle-safe:** the new `tsn_load_* → tsn_library` edge is acyclic (`tsn_library` never imports `tsn_load_*` — builders resolve lazily via `report_catalog` strings; `check_import_direction` green). **59/59** Python suite (58→59) + 2 Node frontend checks + byte-compile + `node --check app.js` + import-direction + app_modules + no-misspelling + `git diff --check` green. `compare_core` / the `compare_*_tsn` comparators + their projection/schema / the 2 multi-file TSN consolidators (`consolidate_tsn_highway_log`/`_highway_sequence`, a different shape) / auth / updater-TLS / `version.py` / `app.js` / matrix `row_key` untouched; no `app.spec` change (all 5 modules already in `APP_MODULES`; checks aren't shipped). **Deviations:** family 2 (`compare_tsn_common.py` comparator driver) **deferred per the plan's explicit P5 note** ("ship the tsn_load factory if time permits; defer the comparator driver to a point release without affecting the DoD"); added `check_tsn_normalizer` beyond the plan's "rely on existing checks" (a substrate-locking strengthening). **Owed:** the COM/Route-1 harness on real TSN pairs (work-PC; §M; not DoD — family 1 doesn't touch the comparison path, so the vs-TSN canaries are unaffected by construction and stay green offline). Files: 1 new test + `scripts/tsn_library.py` + 4 `tsn_load_*` shims + `.github/workflows/checks.yml`. **Next eligible phase: P6** (`pending`; blocking core, depends P0 ✓) — with **P7a/P8a** (independent) + the now-unlocked **P9/P10** also eligible; P5 family-2 deferred. **Codex round 1 = `PASS WITH FIXES`** (no blockers; 1 required **P5-R01** + 1 rec **P5-A01**; Codex's own 6-case semantic harness matched 6/6 → both are tripwire/wording, not defects) → **both resolved (round-1 remediation):** **P5-R01** the PERMANENT `check_tsn_normalizer` now locks — against an **independent frozen oracle** (sheet/header/alignment literals hand-written, NOT re-read from the shim's kwargs) — every dimension the one-off harness compared: each of the 6 cases' every `ConsolidateResult` field + exact event-log line + sheet/header/data rows + header alignment **+ font + fill**, plus the exact shared error/cancel strings and the atomic-save `PermissionError` contract (prior artifact kept, no stray file); report wording corrected to **semantically identical** (4× "byte-identical" removed) and the permanent check distinguished from the one-off harness; **26→28** assertions, RED-proven (wrong fill → font/fill FAIL; backstop removed → P5-A01 FAIL). **P5-A01** (applied) a **centralized** ImportError backstop in `build_normalized` returns the friendly `deps_msg` if a partial/frozen-pruned openpyxl lacks `WriteOnlyCell`/`Alignment`/`Font`/`PatternFill` (never crashes), + a focused negative. This round touched `scripts/tsn_library.py` (backstop) + `build/check_tsn_normalizer.py` (rewrite) ONLY — the 4 shims / `compare_core` / comparators / `app.spec` untouched. **59/59** Python suite + 2 Node frontend checks + byte-compile + `node --check` + import-direction + app_modules + no-misspelling + diff-check green; no `REVERT-PROOF`. Round-1 remediation appended to the report; phase kept `awaiting_review`. **Codex round 2 = `PASS WITH FIXES`** (no blockers; **P5-A01 confirmed Resolved**; 1 required **P5-R01** narrowed to the save-error tripwire — Codex's own real-`atomic_save` diagnostic preserved the prior + left no temp, so a noisy/missing tripwire, not a defect) → **resolved (round-2 remediation):** the round-1 save-error test patched `atomic_save` itself to raise (bypassing the real temp-cleanup), checked `raw.iterdir()` (the INPUT dir, but temps live beside `out_path`), and left a write-only workbook unsaved → ignored openpyxl/lxml atexit exceptions after the pass banner (making "all green/ASCII-clean" misleading). **Fix** (`build/check_tsn_normalizer.py`, save-error case only — NO product change): the test now keeps the REAL `artifact_store.atomic_save` and patches `artifact_store.os.replace` to raise `PermissionError`, so `atomic_save` runs (saves the workbook to a `.tmp-*` sibling → no atexit noise; `os.replace` raises; `_silent_unlink` removes the temp; the factory's `except PermissionError` returns the exact friendly message); asserts the prior output **bytes retained** + the OUTPUT dir has **no `.tmp-*` sibling** + keeps the exact message assertion. **RED-proven** (with `_silent_unlink` no-op'd a real `keep.tmp-*.xlsx` survives → the assertion fails). Verified **clean exit: `python build/check_tsn_normalizer.py` exits 0 with EMPTY stderr** (atexit noise gone). `check_tsn_normalizer` stays **28** assertions. **59/59** Python suite + 2 Node frontend checks + byte-compile + `node --check` + import-direction + app_modules + no-misspelling + diff-check green; no `REVERT-PROOF`. This round touched `build/check_tsn_normalizer.py` ONLY (no product change). Round-2 remediation appended to the report; phase kept `awaiting_review`. **Codex round 3 = `PASS`** (no blocking/required/recommended findings; P5-R01 + P5-A01 both Resolved). **Committed `c0cfa39`** "refactor: collapse TSN single-file normalizers into a shared factory (S04)" — the **7** intended product files staged by explicit path (**1 new:** `build/check_tsn_normalizer.py`; **5 modified product:** `scripts/tsn_library.py` + the 4 `tsn_load_*` shims; **1 CI:** `.github/workflows/checks.yml`); `docs/planning/` NOT staged; **not pushed**. Final verification at commit: `check_tsn_normalizer` 28 assertions GREEN with **empty stderr** (clean exit), the 6 TSN canaries + **59/59** offline Python suite + 2 Node frontend checks + byte-compile + `node --check app.js` + import-direction + app_modules + no-misspelling + `git diff --check` clean; no `REVERT-PROOF`. `compare_core` / the `compare_*_tsn` comparators+schemas / the 2 multi-file TSN consolidators / auth / updater-TLS / `version.py` / `app.js` / `app.spec` / matrix `row_key` untouched. **Owed:** the COM/Route-1 harness on real TSN pairs (work-PC; §M; not DoD — family 1 doesn't touch the comparison path). **P5 family 2** (the `compare_tsn_common.py` comparator driver) **deferred** per the plan's explicit P5 note (point release; not in the DoD). **Next eligible phase: P6** (`pending`; blocking core, depends P0 ✓) — with **P7a/P8a** (independent) + **P9/P10** (need P4 ✓ + PA ✓) also eligible. |
| **P6** | `committed` (Codex round 2 `PASS`) | `c0cfa39` | [`phases/P6-claude-report.md`](phases/P6-claude-report.md) | Persistence hardening (narrowed; DPAPI→O2; R1-N02). Baseline `c0cfa39` (P5 committed); branch `refactor/v0.18.0-structural-overhaul`. **Three evidenced hardenings, no persisted-format change:** **(1) settings writer dedup** — the 4 writers that still inlined `tempfile.mkstemp`+`os.replace` (`update`/`set_site_url`/`set_batch_dest`/`set_matrix_baseline`) now route through the single `_atomic_write` (moved to the top; their duplicated blocks + unused `global _cache` removed); unknown-key round-trip + corrupt-move-aside untouched. **(2) atomic auth-at-rest write + owner ACL** (`common.save_auth_state`) — now `tempfile.mkstemp`+`os.replace` (F9, never truncates a prior session) + new `_restrict_to_owner` best-effort tightens the NTFS ACL to the current user via the built-in **`icacls`** (`/inheritance:r /grant:r <user>:F`, list-form/no-shell, `CREATE_NO_WINDOW`, any failure logged+ignored → no lock-out); **NOT DPAPI** (deferred O2 — DPAPI binds user+machine + breaks `storage_state` portability; the file stays plaintext JSON, bytes unchanged). **(3) support-bundle settings allowlist** — new explicit `_SUPPORT_BUNDLE_KEYS` + `settings.support_bundle_settings()` (⊆ DEFAULTS via an import-time assert); `gui_api`'s bundle manifest uses it, not `all_settings()`, so a future sensitive setting isn't auto-shared (site_urls/batch_dest already excluded). **Per R1-N02 deliberately NOT done:** `_safe_join`, `full_snapshot()`, `paths` init-rewrite, settings schema-version (no concrete migration). **Security review (security-reviewer agent over the diff) — no CRITICAL/HIGH; 2 findings APPLIED:** MEDIUM — validate `USERNAME` against the Windows-forbidden charset before it flows into the icacls `account:perm` arg (a stray `:`/`\` would silently mis-parse); LOW — apply the ACL to the **temp BEFORE the rename** so AUTH is never briefly broad. Atomic-write/list-form-subprocess/temp-cleanup/allowlist confirmed correct; bundle confirmed to exclude `tsmis_auth.json` + `site_urls`/`batch_dest`. **New `build/check_persistence.py`** (16 assertions, ASCII-clean, **RED-proven** — run against the baseline `settings`/`common`/`gui_api` it fails: dedup-routing + the auth ACL path are absent) locks all 3 deliverables + the 2 security hardenings; wired blocking into `checks.yml` after `check_gui_bridge`. **60/60** Python suite (59→60) + 2 Node frontend checks + byte-compile + `node --check app.js` + import-direction + app_modules + no-misspelling + `git diff --check` green. `compare_core`/matrix/engine/updater-TLS/`paths`/`report_catalog`/`tsn_library`/`app.spec`/`version.py` untouched; no persisted-format change (config.json + tsmis_auth.json shapes unchanged). **Owed (work-PC; §M; not DoD):** real `icacls` ACL behavior on the managed Caltrans PC + a live sign-in writing/reading the ACL-locked file; DPAPI at-rest = **O2**. **Doc:** `docs/it-and-security.md` auth-rest narrative (still true — owner ACL is an NTFS perm; DPAPI still the deferred candidate) reconciled in **P11** (plan scopes P6 to code). Files: 1 new test + `settings.py` + `common.py` + `gui_api.py` + `checks.yml`. **Next eligible phase: P7a** (`pending`; GUI state owner + lifecycle; depends P1 ✓ + PA ✓) — with **P8a** (independent) + **P9/P10** (need P4 ✓ + PA ✓) also eligible. **Codex round 1 = `PASS WITH FIXES`** (no blockers; 1 required **P6-R01** + 1 rec **P6-A01**) → **both resolved (round-1 remediation):** **P6-R01** `_restrict_to_owner` discarded the `subprocess.run` result, so a non-zero `icacls` return (a real ACL failure, not an exception) was SILENT — contradicting the "failure logged and ignored" contract; now it captures the result (`PIPE`+merged stderr) and `log.info`s a non-zero return (rc + first output line) as a best-effort failure while `save_auth_state` still continues (file keeps its inherited owner-readable ACL); a new check assertion (stubbed `rc=5`) locks it, **RED-proven** (pre-fix discard-result fails it). **P6-A01** (applied — in-scope product wording, not deferred) the support-bundle docstring + manifest NOTE + completion log now say "selected diagnostic settings"/"allowlisted" to match the allowlist change (the manifest already embeds `support_bundle_settings()`); broader `docs/` narrative still P11. `check_persistence` **16→17** assertions; ASCII-clean. No change to `settings.py` / the atomic-write mechanics / the allowlist set / any persisted format. **60/60** Python suite + 2 Node frontend checks + byte-compile + `node --check` + import-direction + app_modules + no-misspelling + diff-check green; no `REVERT-PROOF`. This round touched `common.py` (rc logging) + `gui_api.py` (wording) + `build/check_persistence.py` (the assertion). Round-1 remediation appended to the report; phase kept `awaiting_review`. **Codex round 2 = `PASS`** (no blocking/required/recommended findings; P6-R01 + P6-A01 both Resolved). **Committed `8d9ec2e`** "fix: harden persistence (atomic auth ACL, settings dedup, bundle allowlist)" — the **5** intended product files staged by explicit path (**1 new:** `build/check_persistence.py`; **3 modified product:** `scripts/settings.py` + `scripts/common.py` + `scripts/gui_api.py`; **1 CI:** `.github/workflows/checks.yml`); `docs/planning/` NOT staged; **not pushed**. Final verification at commit: `check_persistence` 17 assertions GREEN with **empty stderr**, `check_gui_bridge` OK, **60/60** offline Python suite + byte-compile + `node --check app.js` + import-direction + app_modules + no-misspelling + `git diff --check` clean; no `REVERT-PROOF`. `compare_core`/matrix/engine/updater-TLS/`paths`/`report_catalog`/`tsn_library`/`app.spec`/`version.py` untouched; no persisted-format change. **Owed (work-PC; §M; not DoD):** real `icacls` ACL behavior on the managed PC + a live sign-in writing/reading the ACL-locked file; DPAPI at-rest = **O2**. **Doc:** `docs/it-and-security.md` auth-rest narrative reconciled in **P11**. **Next eligible phase: P7a** (`pending`; GUI state owner + protocol + lifecycle + enum SSOT; depends P1 ✓ + PA ✓) — with **P8a** (independent) + **P9/P10** (need P4 ✓ + PA ✓) also eligible. |
| **P7a** | `committed` (Codex round 2 `PASS`) | `8d9ec2e` | [`phases/P7a-claude-report.md`](phases/P7a-claude-report.md) | GUI state owner + protocol + lifecycle + enum SSOT (F8/F14/R1-R06/R14/D03). Baseline `8d9ec2e` (P6 committed); branch `refactor/v0.18.0-structural-overhaul`. **State boundary established BEFORE any endpoint move (P7b), behind the GuiApi façade — JS API names + event order byte-identical.** **(1) new `scripts/task_coordinator.py`** = the single owner of the single-flight gate (`task`), running matrix job, matrix queue, job-id counter, with atomic `try_claim`/`release`/`owns`/`enqueue`/`take_next`/`next_seq`/`is_busy`/`depth` under the shared `RLock`. **(2) `gui_api` delegates:** `_task`/`_current_job`/`_queue`/`_job_seq` are now thin property proxies to the coordinator (the ~50 incidental accesses unchanged); the lifecycle methods (`_try_claim_task`/`_release_task`/`_end_task` gate-clear/`_enqueue_matrix_job`/`_try_start_next_matrix_job`/`_make_job`) delegate to it. **(3) exactly-once lifecycle (R1-R14):** `_handle` drops a TERMINAL whose task no longer owns the gate (`coordinator.owns()` + a `_TERMINAL_TASK` map), so a straggler/late terminal from a just-finished task can't clobber a successor that already took the gate — **flips the duplicate-late gap CT-10 previously LOCKED (D21)** (workers post one terminal each — `test_producer_paths` — so the guard is the defensive net). **(4) `_handle` → dispatch table:** the 27-branch if/elif → a `kind→handler` dict (keyed by `contract.Msg.*`), the 10 inline branches extracted to `_on_*` methods (bodies verbatim); unknown kind → `log.warning` (no silent drop). **(5) new `scripts/contract.py`** = bridge enum SSOT (`Task`/`Msg`+`TERMINAL`/`LoginPhase`/`ErrorKind`/`EnvAccess`, naming the EXISTING strings — behavior-neutral), surfaced via `get_initial_state` → `contract.initial_state_enums()` (an additive key; ui/contract.js consumes it in P9). **(6) named swallows logged:** login `_safe_close`/`_safe_close_context` (gui_worker), `_chromium_state` size probe, support-bundle zip writes (gui_api) now log type+first-line instead of bare `pass`. `app.spec` `APP_MODULES += contract, task_coordinator` (F6 — `check_app_modules` caught the omission, red→green). **CT-10 rewritten + GREEN:** the duplicate-late-active-successor test now asserts the straggler is a NO-OP (successor keeps gate + running job) + an unrelated-kind straggler is also dropped; RED-proven (the guard makes the prior clobber-asserting test fail). **Behavior-neutral, verified:** all 4 bridge checks (`check_gui_bridge`/`check_matrix_bridge`/`check_day_matrix`/`check_b3_batch`) + `check_intersection_gate` green; dispatch covers exactly the 27 old kinds (0 uncovered/extra); JS API + event order unchanged (handlers verbatim; the guard only drops production-impossible stragglers). **60/60** Python suite + 2 Node frontend checks + byte-compile + `node --check app.js` + import-direction + app_modules + no-misspelling + diff-check green; no `REVERT-PROOF`. `compare_core`/matrix/engine/updater/`ui/*`/`version.py` untouched; no persisted-format change. **Deviations:** exactly-once is kind-guarded at the dispatch (not per-job generation tokens) — correct for all reachable flows (queue holds only matrix jobs → successor is always a different kind; one terminal per worker); the same-kind matrix→matrix straggler is unreachable (documented). No endpoint files split (P7b). **Owed (external; §M; not DoD):** manual `#mock` all-tabs smoke + a live GUI run on the work PC (P7a is backend-only — `ui/app.js` untouched, the `#mock` runs a stubbed bridge, so it's unaffected by design); `docs/gui-bridge`/internals reconciliation → P11. Files: 2 new (`contract.py`, `task_coordinator.py`) + `gui_api.py` + `gui_worker.py` + `app.spec` + `check_worker_lifecycle.py`. **Codex round 1 (`BLOCKED`) remediated (this changeset):** P7a-B01 re-architected exactly-once from the kind-guard to a per-claim **epoch** (`is_live`/`_StampedQueue`; closes generic `error`/`cancelled` + same-kind `matrix`->`matrix` stragglers; the §10 deviation is resolved); P7a-R01 `#mock` all-tabs smoke run+recorded; P7a-A01 dispatch/terminal-set parity assertion added; CT-10 -> 11 groups (RED-proven); full blocking suite green; same 6-file diff. See report §Remediation. **Committed `42122ff`** (6 files: 2 new + 4 modified; planning folder not staged; not pushed). **Next eligible phase: P8a** (`pending`; engine leaf extraction; depends PA ✓) — with **P7b** (conditional; now unlocked, depends P7a) + **P9/P10** (need P4 ✓ + PA ✓) also eligible. |
| **P8a** | `committed` (Codex round 1 `PASS`) | `42122ff` | [`phases/P8a-claude-report.md`](phases/P8a-claude-report.md) | Engine leaf extraction (R1-R08 / §E DAG / F14). Baseline `42122ff` (P7a committed). **Four pure L0 leaves extracted from `common.py` behind a re-export SHIM, behavior-neutral, no lock (R1-R07):** `errors` (exception types), `site_target` (site/env selection + report URL), `timeouts` (defaults + Settings accessors), `routes` (list + parser) — bodies VERBATIM; `common.py` 1713->1435 lines, re-imports every name so `from common import X` is byte-identical (the 14-module import surface). **Shim parity proven** (`common.X is leaf.X`, 35 identities + the shared `tsmis.auth` logger). New `build/check_engine_leaves.py` (leaf behaviors + parity); `APP_MODULES` 57->61 (F6); `check_export_engine` site-pin -> public `set_site`; `check_engine_leaves` wired into `checks.yml`. Import graph still acyclic; full **63** `check_*.py` + 2 Node + `app.js` + byte-compile green. **Deviations (documented, §10):** §E's `site.py` -> `site_target.py` (a flat `site.py` is shadowed by the stdlib `site` module — verified); **auth-file lifecycle DEFERRED to P8b** (§E places it in `auth_nav`; tightly coupled to `check_persistence`'s `common.subprocess`/`os`/`AUTH` P6 patches — moving now defeats them + double-moves; P8a's `(+ auth-file lifecycle)` reconciled toward §E + low-risk + no-churn). Files: 5 new (4 leaves + `check_engine_leaves.py`) + `common.py` + `app.spec` + `check_export_engine.py` + `checks.yml`. Frozen `--self-test` exe gate runs in CI (offline reachability `check_app_modules` green); docs reconciliation -> P11. Planning folder not staged. **Committed `bdbda4d`** (9 files: 5 new + 4 modified; not pushed). **Next eligible phase: P8b** (`pending`; engine mechanical movement along the DAG; depends P8a) — with **P7b**/**P9**/**P10** also eligible. |
| **P8b** | `committed` (Codex round 2 `PASS`) | `bdbda4d` | [`phases/P8b-claude-report.md`](phases/P8b-claude-report.md) | Engine mechanical movement along the verified acyclic DAG (R1-R08 / R1-B03 / D04 / §E / F14). Baseline `bdbda4d` (P8a committed). **The rest of `common.py` — the field-hardened browser / auth / Edge-device / session flows — moved into 5 flat layer modules behind the shim, behavior-neutral, no lock:** `browser_channels` (L1), `auth_nav` (L2, **+ the P8a-deferred auth-file lifecycle**), `report_nav` (L2', above auth_nav), `edge_device` (L3), `session` (L4). Every moved body is **AST-VERBATIM** vs baseline (0 mismatches, 0 left behind, 0 dupes); `common.py` 1435->58 lines (pure re-export shim, import surface byte-identical, shared `tsmis.auth` logger). New `build/check_engine_layers.py` (shim parity 39 ids + no-upward-import + DAG layering + thread-agnostic engine), wired into `checks.yml`; `APP_MODULES` 61->66 (F6). 3 tests retargeted to the moved homes (`check_persistence`/`check_export_engine` -> auth_nav; `check_gui_bridge` -> browser_channels). Import graph acyclic; full **64** `check_*.py` + 2 Node + `app.js` + byte-compile green; all 70 flat modules import; `check_fake_site` (real selector contract) green. **Deviations (documented, §10):** §E's 'independent L2/L2' siblings' was inaccurate — a real `auth_nav<->report_nav` cycle (`auth_state->page_url_for_display`, `preflight->dump_auth_failure`); resolved by homing the §E-unplaced `page_url_for_display` with its sole caller `auth_state` in auth_nav (no body edited), making report_nav a one-way dependent — acyclic. 'thread-affinity assertions' realized as a structural lock (check_engine_layers thread-agnostic) + the verbatim move; live thread-affinity = work-PC/external. Files: 6 new (5 layers + `check_engine_layers.py`) + `common.py` + `app.spec` + 3 tests + `checks.yml`; one-shot generator deleted. Frozen `--self-test` exe gate -> CI; docs reconciliation -> P11. Planning folder not staged. **Codex round 1 (`PASS WITH FIXES`) R01 remediated (this changeset):** the shim + parity check (`check_engine_layers::OWNS`) now also re-export/lock the two §E-named public DAG helpers `dump_auth_failure` (auth_nav) + `open_edge_device_context` (edge_device) — shim 61->63 names, parity 39->41 ids; bodies untouched (AST still verbatim), suite still green. See report §Remediation. **Committed `bb144d4`** (12 files: 6 new + 6 modified; not pushed). **Next eligible phase: P9** (`pending`; frontend mock separation; depends P4 + PA) — with **P7b**/**P10**/**P8c**(O7) also in play. |
| **P9** | `committed` (Codex round 2 `PASS`) | `bb144d4` | [`phases/P9-claude-report.md`](phases/P9-claude-report.md) | Frontend mock separation (F13 / R1-R09/N03/R13). Baseline `bb144d4` (P8b committed). **Killed the mock-as-2nd-backend drift: the `#mock` browser-preview API moved VERBATIM out of `app.js` into its own `ui/mock.js` (owns the mock boot, RR2-C3); app.js auto-boots only in production (`if (!WANT_MOCK)`).** app.js 5054->3705 lines; **lines 1-3606 byte-identical** (no production-UI change). New `ui/contract.js` (the `window.CONTRACT` bridge-enum mirror of `contract.initial_state_enums`), loaded before app.js; the mock's `get_initial_state` returns `contract: window.CONTRACT` (payload parity). `index.html`: classic-`<script>` order contract.js -> app.js -> **`#mock`-gated `mock.js`** (createElement/appendChild, after app.js). New `build/check_ui_contract.py` (contract.js == contract.py enums) + `build/check_ui_boot.js` (syntax + boot structure + no missing globals + index.html ordering/404s), both wired into `checks.yml`; `check_report_catalog::test_mock_parity` (CT-13 report-payload parity) retargeted app.js -> mock.js. **#mock browser smoke recorded:** boots, 3 scripts load, `S.init.contract.tasks==10`, all tabs + matrix render, 0 console logs. Full **65** `check_*.py` + **3** Node + UI syntax + byte-compile green; all RED-proven. **Deviations (§10):** 'fixtures generated from report_catalog' realized as the test_mock_parity LOCK (no build step / no generated file); CT-13 split into report-parity (check_report_catalog) + enum-parity (check_ui_contract), no duplication; gate uses createElement not document.write (parse-timing robust); contract.js runtime adoption deferred to O1 (R1-N03). Files: 4 new (mock.js, contract.js, 2 checks) + app.js + index.html + check_report_catalog + app.spec + checks.yml; generator deleted. Frozen `--self-test` production boot -> CI; docs/gui.md reconciliation -> P11. Planning folder not staged. **Codex round 1 (`PASS WITH FIXES`) R01 remediated (this changeset):** the `pywebviewready` listener moved INSIDE the `if (!WANT_MOCK)` gate, so EVERY real-bridge boot path is production-only (under `#mock` app.js registers no bridge boot -> mock.js owns it); `check_ui_boot.js` now brace-matches the gate + asserts every `boot(window.pywebview.api)` + the listener are inside (RED-proven). app.js 1-3606 still byte-identical; #mock re-smoke green (0 console logs). See report §Remediation. **Committed `decced4`** (9 files: 4 new + 5 modified; not pushed). **Next eligible phase: P10** (`pending`; packaging/deps/updater hardening; depends PA + P4) — with **P7b**/**P8c**(O7) also in play. |
| **P5b** | `committed` (Codex round 1 `PASS`) | `decced4` | [`phases/P5b-claude-report.md`](phases/P5b-claude-report.md) | TSN comparison-driver DRY — the CR-001 comparator remainder of P5 (RM01; committed P5 NOT reopened). New `scripts/compare_tsn_common.py` (149 lines): **`run_files_compare`** (the shared deps/paths/banner/load/`run_compare` `compare()` driver), **`make_notes_writer`** (the Highway Sequence + Intersection Detail "Notes" legend), and **`norm_pm`/`iso_date`** (the shared FLAT-detail normalizers). The **5** `compare_*_tsn` reduced to schema + projector (1380→1219 lines, **−161**): each `compare()` is now a thin `ctc.run_files_compare(...)` call; the 2 detail modules alias `_norm_pm`/`_iso_date` to ctc; the 2 notes modules set `_write_notes_sheet = ctc.make_notes_writer(...)`. Every preserved name (canary internals, `tsn_load_*` imports, registry surface) resolves unchanged; **`compare_core` untouched**. New `build/check_compare_tsn_common.py` (substrate behavior + delegation lock) wired into `checks.yml`; `compare_tsn_common` added to `app.spec` APP_MODULES (F6). **RED-proven** (16 delegation asserts failed pre-refactor) → GREEN. Verification: **66/66** `check_*.py` (65→66) + **3** Node + byte-compile + `git diff --check` clean; the **5 vs-TSN canaries semantically identical** before/after; `check_tsn_normalizer`/`check_app_modules`/`check_import_direction` green. **Deviation (§10):** "header helpers" delivered as normalize+notes+driver; no shared consolidated-sheet loader (per-report position maps differ — KISS); `iso_date` unified to the 3-case superset (neutral for Ramp Detail). **Owed:** Route-1/COM-recalc acceptance stays external/work-PC (RM07) — but P5b changes no comparison behavior, so the existing Route-1 lock is unaffected. **Committed `8eb9cc8`** "refactor: collapse the 5 vs-TSN comparators onto a shared driver (P5b)" — the 9 intended product files staged by explicit path (2 new: `compare_tsn_common.py` + `build/check_compare_tsn_common.py`; 7 modified: the 5 `compare_*_tsn` + `app.spec` + `checks.yml`); `docs/planning/` NOT staged; **not pushed** (`origin/main` still `d2ee353`). Final verification at commit: the 5 vs-TSN canaries + `check_compare_tsn_common` + `check_tsn_normalizer`/`check_app_modules`/`check_import_direction`/`check_report_catalog` green, byte-compile + `git diff --check` clean. Planning folder not staged. **Next eligible phase: P7b** (`pending`; depends committed P7a) — with P8c/P9b/P10/P12 also in play. |
| **P7b** | `committed` (Codex round 2 `PASS`) | `8eb9cc8` | [`phases/P7b-claude-report.md`](phases/P7b-claude-report.md) | GUI mechanical endpoint extraction — reclassified conditional → blocking (CR-001/RM08). Two behavior-neutral extractions out of the 3741-line `gui_api.py` bridge: new **`scripts/gui_win32.py`** (the 4 pure ctypes/Win32 helpers `find_own_window`/`set_window_icon`/`flash_taskbar`/`message_box`, **zero coupling** — gui_api keeps the gate/poll/log orchestration; `import ctypes` removed from gui_api), and a new **`_begin_compare`** helper unifying the identical claim→dialog→launch→release tail of `start_compare`/`start_compare_env` (the plan's `_begin_task`/compare-unify; a generic `_begin_task` over the 17 heterogeneous claim sites would be speculative over-abstraction — KISS/RM08). New **`build/check_gui_api_surface.py`** locks the **98-name pywebview façade unchanged** (RM08) + the 2 endpoints' source signatures + the win32 delegation + `_begin_compare` (2 call sites, 1 collapsed claim tail); wired into `checks.yml`; `gui_win32` added to `app.spec` (F6). `gui_api.py` 3741→3680. **RED-proven** on baseline (`8eb9cc8` has 0 gui_win32 refs, 8 inline Win32 markers, 0 `_begin_compare`). Verification: **67/67** `check_*.py` (66→67) + **3** Node + byte-compile + `git diff --check` clean; bridge golden checks (`gui_bridge`/`matrix_bridge`/`b3`) + `app_modules` + `import_direction` green; **no `scripts/ui/` file touched** (façade identical → #mock unchanged, frontend-structure verification N/A). `compare_core`/auth/updater untouched. **Deviation (§10):** the **Matrix feature-endpoint module grouping is deferred to the next P7b group-diff** (the plan's own "one group per diff / per-group revert / medium risk" framing + behavior-neutrality on the field-hardened bridge); Codex to rule whether it must land within P7b. Backend-only → no work-PC item. **Codex round 1 (`PASS WITH FIXES`) remediated:** **R01** → the Matrix endpoint grouping is formalized as new blocking phase **P7c** (Codex's sanctioned amendment path — its own baseline/review/`#mock` gate, not a rushed remediation cut; matches "one group per diff" + the user's "add as many phases as needed"); **R02** → `#mock` recorded N/A for backend-only P7b (P7c carries the all-tabs smoke); **A01** → `_begin_compare` suggest-name made a **lazy callable** evaluated inside the claim (restores the pre-P7b claim-before-suggest ordering) + 2 new `check_gui_api_surface` asserts. **67/67** Python + 3 Node + bridge golden checks re-green; report §Remediation appended. **Codex round 2 = `PASS`** (no blocking/required; one non-blocking P7b-A02 planning-text cleanup, applied). **Committed `07b846b`** "refactor: extract gui_win32 + unify the compare-start tail (P7b)" — the 5 intended product files staged by explicit path (2 new: `scripts/gui_win32.py` + `build/check_gui_api_surface.py`; 3 modified: `gui_api.py` + `app.spec` + `checks.yml`); `docs/planning/` NOT staged; **not pushed** (`origin/main` still `d2ee353`). Planning folder not staged. **Next eligible phase: P7c** (`pending`; the Matrix grouping; depends committed P7b) — then P8c/P9b/P10/P12. |
| **P7c** | `committed` (Codex round 1 `PASS`) | `07b846b` | [`phases/P7c-claude-report.md`](phases/P7c-claude-report.md) | GUI Matrix feature-endpoint grouping (CR-001/P7b-R01) — the largest mechanical move in v0.18.0. The cohesive Matrix / by-day / TSN-library cluster (`_valid_baseline` … `_on_matrix_export_done`, **71 methods / ~1048 lines**) moved **VERBATIM** out of `GuiApi` into a new **`scripts/gui_matrix.py` (`GuiMatrixMixin`)**; `GuiApi(GuiMatrixMixin)` inherits it so the pywebview façade is byte-identical (MRO preserves names/return-shapes/event-order); `task_coordinator` stays the state owner. New **`scripts/gui_endpoint.py`** homes `_api_method` to break the `gui_api`↔`gui_matrix` cycle (the "small shared decorator module" the plan anticipated). `gui_api.py` **3684→2614 (−1070, −29%)**; the block's now-unused imports trimmed (4 matrix workers + `collections`/`matrix`/`day_matrix`/`matrix_rows`); the 5 **lazy `import tsn_library`** preserved verbatim; `_matrix_export_running` left in `gui_api` (shared by `request_preview`). **AST-verbatim proven** (one-off verifier: 71 methods, **0 differences**; **0 missing imports**). `check_gui_api_surface` **extended** (moved cluster lives in `gui_matrix`, gone from `GuiApi`, still resolves via the mixin, public endpoints stay in the frozen 98-name façade); `check_matrix_bridge`/`check_day_matrix` retargeted to patch the matrix workers on `gui_matrix` (RED→GREEN: the gui_api patches no longer intercepted the moved dispatch); `app.spec` += `gui_endpoint`/`gui_matrix` (F6). Verification: **67/67** `check_*.py` + **3** Node + byte-compile + import-direction + reachability + `git diff --check` clean; **`#mock` all-tabs smoke** green (boots, 9 tabs, Comparison-matrix 32 cells + vs-TSN-matrix 32 cells render, contract enum=10, **0 console errors**). `compare_core`/auth/updater untouched. **Deviation (§10):** the matrix dispatch *pairs* were MOVED verbatim, **not unified** (unifying would break the AST-verbatim zero-behavior guarantee — RM08; offered as a follow-up); `_matrix_export_running` left in `gui_api` (shared). Backend-only → no work-PC item. **Codex round 1 = `PASS`** (no blocking/required/recs; the dispatch-pair verbatim-not-unified deviation explicitly accepted). **Committed `9121faa`** "refactor: extract the GUI matrix cluster into a gui_matrix mixin (P7c)" — the 7 intended product files staged by explicit path (2 new: `scripts/gui_matrix.py` + `scripts/gui_endpoint.py`; 5 modified: `gui_api.py` + `check_gui_api_surface.py` + `check_matrix_bridge.py` + `check_day_matrix.py` + `app.spec`); `docs/planning/` NOT staged; **not pushed** (`origin/main` still `d2ee353`). Planning folder not staged. **Next eligible phase: P8c** (`pending`; engine behavior, v0.18.1 acceptance; depends committed P8b + fake-site fixture) — then P9b/P10/P12. |
| **P8c** | `awaiting_review` | `9121faa` | [`phases/P8c-claude-report.md`](phases/P8c-claude-report.md) | Engine behavior changes — the live-path correctness/security set, reclassified conditional → blocking-code (CR-001/RM02/RM03): **code ships v0.18.0, work-PC acceptance → v0.18.1**. **Fixture-first per RM03** (3 new `build/fake_site/*` + RED tests landed before each behavior change). **Six independently-revertible changes:** **(1) `select_report` exact-match guard** (AUDIT-P2-substr) — `report_nav._find_exact_option` enumerates `#customReport li.cs-option` + matches trimmed text EXACTLY, raising `PreflightError` on 0/multiple (replacing the `has_text` SUBSTRING + `.first` that silently mis-picked when one label ⊂ another, e.g. "Highway Log" ⊂ "Highway Log (PDF)"); `preflight` gains `except PreflightError: raise` to keep the precise message + dump; cs-disabled unchanged. **(2) per-route stale-form re-arm** (select-report-not-rearmed) — `report_nav.current_report_label` reads `.cs-value`; `exporter._ensure_report_armed` (top of `_attempt_route`) re-selects on drift, no-ops on the happy path, never acts on `''`=unknown. **(3) Edge-login CDP default-off / on-demand** (AUDIT-P2-cdp) — `launch_edge_login_context(enable_cdp=False)` no longer opens the unauthenticated `--remote-debugging-port` during interactive sign-in (opt-in only); `capture_edge_login_state_over_cdp` no-ops without a port → profile-recapture fallback (no CDP exposure). **(4) `should_cancel` into recover/retry/portability + login busy-waits** (`should_cancel`-in-recover, login-busywait) — `exporter._recover` (wired via `_recover_or_stop`→`events.is_cancelled`), `edge_device.storage_state_is_portable`/`capture_edge_login_state_over_cdp`/`capture_edge_login_state_from_profiles` poll a new `should_cancel` (GUI `LoginWorker` threads `self.cancel.is_set`) so a Stop during the post-"I've finished" capture/portability chain bails ~1s instead of ~90s; **only polling ADDED, no hardened wait chunked**. **(5) marker-independent PDF empty backstop** (unlogged-no-download-empty-on-pdf) — `save_pdf_letter` (no action bar) + `save_highway_log_pdf` (zero non-spanning data rows) raise `EmptyExport` (logged) so a drifted empty-TEXT marker can't save a contentless PDF. **(6) auth-path swallow logging** — the recapture-navigate (edge_device), arcgis-click (auth_nav `note()`), and console live-capture (login.py) swallows now log `type(e).__name__`. **New keyword params all default to no-op** (`enable_cdp=False`/`should_cancel=None`) so every existing caller (incl. console `login.py`) is behavior-unchanged; no new shipped module → `app.spec` unchanged; **`compare_core`/auth-at-rest/updater/`scripts/ui/` untouched**. New `build/check_edge_login.py` (CDP gating + cancel + swallow, pure-Python fakes) wired into `checks.yml`; `check_export_engine`/`check_fake_site` extended (exact-match + re-arm + PDF-empty, pure-Python + real-browser); `check_worker_lifecycle` stub tolerates the new kwarg. **RED→GREEN proven** per change (stash product file → FAIL → restore → GREEN). Verification: **68/68** `check_*.py` (67→68) + **3** Node + byte-compile + import smoke + `git diff --check` clean; `check_gui_api_surface`/`check_gui_bridge` unaffected (no bridge/UI change). **Owed (work-PC; §K2/§M; v0.18.1):** live select/CDP/cancel/PDF-empty acceptance on the managed PC. Diff: 7 scripts (`report_nav`/`common`/`exporter`/`edge_device`/`auth_nav`/`login`/`gui_worker`) + 3 checks + `checks.yml` + 4 new (`check_edge_login.py` + 3 fixtures). Planning folder not staged; not committed; not pushed. **Codex round 1 + round 2 = `PASS WITH FIXES`** (no blocking; one Required **P8c-R01** — cancel didn't reach the NESTED recapture `navigate_with_auth`). **P8c-R01 remediated (this round, FIXED):** `capture_storage_state_if_logged_in` gains `should_cancel=None` and passes it into its `navigate=True` `navigate_with_auth`; both recapture callers thread their callback into it; `check_edge_login` gains `test_cancel_reaches_nested_navigate` (records the nested kwarg, drives both CDP+profile paths after a context opens) — **RED-proven** (gap re-introduced → nested gets empty kwargs) → GREEN. Re-ran the Codex-specified set (edge-login/worker-lifecycle/export-engine/engine-layers/app-modules/import-direction/no-misspelling + py_compile + diff-check) + full **68/68** Python + **3** Node green; report §Remediation appended. Phase kept `awaiting_review`. **Codex round 3 = `PASS`** (no blocking/required/recs; P8c-R01 Resolved). **Committed `35a5af0`** "fix: harden export/auth live-path behavior (P8c)" — the **15** intended product files staged by explicit path (4 new: `build/check_edge_login.py` + 3 `build/fake_site/*.html`; 11 modified: 7 scripts `report_nav`/`common`/`exporter`/`edge_device`/`auth_nav`/`login`/`gui_worker` + 3 checks `check_export_engine`/`check_fake_site`/`check_worker_lifecycle` + `.github/workflows/checks.yml`); `docs/planning/` NOT staged; **not pushed** (`origin/main` still `d2ee353`). Committed as one focused phase commit (the six changes remain logically/independently revertible per the report §3; per-change git commits weren't feasible — the changes interleave across shared files and this env has no per-hunk staging). Final verification at commit: the Codex-cited targeted set + **68/68** Python + **3** Node + byte-compile + `git diff --check` clean; commit contains exactly the 15 product files (no `docs/planning`). **Owed (work-PC; §K2/§M; v0.18.1):** live select/CDP/cancel/PDF-empty acceptance on the managed PC. **Next eligible phase: P9b** (`pending`; frontend deeper modularization — renderer merge + app.js split, no framework; depends committed P9) — then P10/P12/P13/P11. |
| **P9b** | `awaiting_review` | `35a5af0` | [`phases/P9b-claude-report.md`](phases/P9b-claude-report.md) | Frontend deeper modularization (CR-001 / D4 / R1-N03 / RM08) — the O1-deferred deeper split, opted in **without framework-izing**. **`scripts/ui/app.js` 3706→2005 (−1701, −46%)** broken into **3 cohesive classic-`<script>` modules** loaded BEFORE app.js (sharing the global lexical scope — the proven `mock.js` pattern): **`ui-dom.js`** (326; DOM/theme/log/modal-dialog primitives + `CHECK_ICON`/`THEME_*`/`modalResolve`), **`ui-matrix.js`** (1008; the comparison + by-day matrix renderers + shared cell/queue/drag helpers + `MX_HI`), **`ui-settings.js`** (386; `fillSettings`…`verifyEnvironment` + `TSN_RAW_KIND_LABEL`). The moved blocks are **byte-for-byte VERBATIM** (one-off verifier: each module body + the app.js remainder == the baseline slices, **0 content differences**; verifier removed). app.js keeps all top-level state (`$`/`S`/`api`/`SETTING_INPUTS`/`WANT_MOCK`), entry points (`boot`/`bindEvents`/`buildStatic`), the top-level handlers, the preview-modal + export/batch/consolidate/compare clusters, and the `!WANT_MOCK` boot gate — so nothing executes at a module's load time referencing a not-yet-loaded symbol (every cross-module ref resolves at call time from `boot()`). **Renderer merge (D4):** the two duplicated `sync*` pairs unified behind `syncMatrixFastControls(cbId,rowId,wkId)` + `syncFormulasToggle(cbId,stateKey)`, the **4 named wrappers preserved** (callers unchanged); the big `renderMatrix`/`renderDayMatrix` render structurally different grids (env vs day columns) + already share the cell/queue helpers, so left distinct (not behavior-neutral-provable to merge — RM08). **`index.html`** classic-script order: contract → ui-dom → ui-matrix → ui-settings → app → (#mock) mock. **No `app.spec` change** (UI ships via the `scripts/ui/*` `.js` asset glob — no APP_MODULES entry). **Tests:** `check_ui_boot.js` extended (compile all 6 scripts; representative fns defined-in-module + GONE-from-app.js + still-called; the 2 merge helpers + 4 wrappers; the 5-script load order; mock last); `check_ui_contract.py` +reference-integrity (every `<script src>` exists); `check_mx_partial_render.js` retargeted (`mxCellContent`/`MX_HI` now in `ui-matrix.js`). **Verification:** byte-exact verbatim proof (0 diffs); `node --check` all 4; **`#mock` all-tabs smoke** — clean boot (`S` populated, contract enum=10, **0 console logs/warnings/errors**), **every cross-module fn resolves** (ui-dom+ui-matrix+ui-settings+app.js), 5 tabs/5 panes/7 report rows, **Comparison matrix 56 cells** + 6 baselines + 7 toggles, Compare 3 sub-tabs (Cross-env/vs TSN/vs TSN Matrix), Settings renders (fillSettings + 6 site-URL + 7 TSN-lib rows); screenshot captured. **68/68** Python + **3** Node + byte-compile + `git diff --check` clean. **Deviation (§10):** renderer merge bounded to the `sync*` pairs; module-boundary lock homed in `check_ui_boot.js` (its natural home) with `check_ui_contract.py` extended per the plan. No production-UI/bridge/contract/`#mock` behavior change; `compare_core`/auth/updater untouched. **Owed (work-PC; §M):** the live pywebview/WebView2 + frozen-bundle boot (same external gate every UI phase carries; the frozen `--self-test` runs it in CI); `docs/gui.md` app.js narrative → P11. Diff: 3 new (`ui-dom`/`ui-matrix`/`ui-settings`.js) + 5 modified (`app.js`/`index.html` + 3 checks). Planning folder not staged; not committed; not pushed. **Codex round 1 = `PASS WITH FIXES`** (no blocking; 1 Required **P9b-R01** + 1 rec **P9b-A01**). **Remediated (this round):** **P9b-R01 FIXED** — the `check_ui_boot.js` "still called" assertion was vacuous (`\bfn\s*\(` matched the declaration itself, missing handler-bound symbols like `verifyEnvironment`, assigned at `app.js:1852`); now it **strips the declaration then requires a remaining reference** (`refBeyondDecl`) + an **explicit `verifyEnvironment ← btnVerifyEnv` handler-wiring lock**; **RED→GREEN proven** (old check passes on an unwired symbol, new catches it, still passes on the real wiring). **P9b-A01 FIXED (report wording)** — softened "only function declarations + literal consts" to "no top-level execution that depends on a later app.js symbol" + noted the self-contained `ui-dom.js` top-level listeners (matchMedia theme-follow + popover-close handlers, baseline-resident, browser-API-only); no product change. Product diff UNCHANGED (only `check_ui_boot.js` + the report touched). Re-ran: `check_ui_boot`/`check_ui_contract`/`check_mx_partial_render`/`check_compare_routing` + `node --check` all 4 + full **68/68** Python + **3** Node green; `git diff --check` clean. Report §Remediation appended. Phase kept `awaiting_review`. **Codex round 2 = `PASS`** (no blocking/required/recs; P9b-R01 + P9b-A01 both Resolved). **Committed `8605eaf`** "refactor: split app.js into cohesive ui-* modules (P9b)" — the **8** intended product files staged by explicit path (3 new: `scripts/ui/ui-dom.js` + `ui-matrix.js` + `ui-settings.js`; 5 modified: `scripts/ui/app.js` + `index.html` + `build/check_ui_boot.js` + `check_ui_contract.py` + `check_mx_partial_render.js`); `docs/planning/` NOT staged; **not pushed** (`origin/main` still `d2ee353`). Final verification at commit: `node --check` all 6 UI scripts + the Codex-cited UI set (boot/contract/mx/compare/app_modules/report_catalog/gui_bridge) + **68/68** Python + **3** Node + `git diff --check` clean; commit contains exactly the 8 product files (no `docs/planning`). **Owed (work-PC; §M):** live pywebview/WebView2 + frozen-bundle boot (the frozen `--self-test` runs it in CI; same external gate every UI phase carries); `docs/gui.md` app.js narrative → P11. **Next eligible phase: P10** (`pending`; packaging/deps/updater hardening — flipped updater items + optional perf; depends committed PA + P4) — then P12/P13/P11. |
| **P10** | `awaiting_review` | `8605eaf` | [`phases/P10-claude-report.md`](phases/P10-claude-report.md) | Packaging / deps / updater hardening (expanded; CR-001 — flipped updater items + optional perf; depends committed PA `65aef98` + P4 `c86dc78`). **Three workstreams, all offline-provable, no protected contract touched (cert-store TLS / two-phase swap order / prune / no new crypto dep all intact).** **(A) `scripts/updater.py` — the §J updater set (9 items, fixture-first RED→GREEN in `check_updater.py`):** (1) **checksum mandatory/fail-closed** — `download_and_stage` refuses to extract with no published checksum (was "size only"), deletes the unverified zip; (2) **zip-slip guard** — `_safe_zip_members` rejects any member escaping the extract dir before extraction; (3) **staged-bundle re-verify (mandatory, whole-bundle, fail-closed)** — `download_and_stage` records a `_bundle_digest` over the ENTIRE staged bundle (`_EXE_NAME` + `_internal/**` + readmes) to a sibling `staged.sha256`; recording is MANDATORY (a compute/write failure removes `UPDATE_DIR` and fails staging — no usable tree left); `apply_update_and_restart` re-verifies the whole bundle in the TRUSTED original process and **fails closed** before `_launch_detached` on a missing/malformed record or any content mismatch (incl. `_internal` tampering) [hardened in remediation round 1 — P10-B01]; (4) **rollback message reflects outcome** — `_rollback_dialog_text` distinguishes full vs PARTIAL restore (no more unconditional "previous version was kept"); (5) **log rotation** — `_rotate_swap_log` caps `update_helper.log` at 256 KB (one `.1` backup); (6) **WebView2 cache-clear below `is_frozen()`** — `cleanup_leftovers` runs store-recovery always, cache-clear frozen-only; (7) **windowed death-check** (CR-001 flip) — `sleep(1.5)`+1 poll → bounded poll (`_DEATH_CHECK_TOTAL_S` 2.0 s / `_INTERVAL_S` 0.25 s), still fail-safe; (8) **download bounded retry** (CR-001 flip) — `_stream_to_file` + `_DL_RETRY_ATTEMPTS`=3 around the socket-timeout stream; (9) **revert pagination** (CR-001 flip) — `_fetch_release_list`/`_next_link` follow `Link: rel="next"` past the 100 cap (`_RELEASES_PAGE_CAP`=50). Signature §J row stays **deferred** (RM06); no runtime signature/new crypto dep (A03). All public signatures unchanged → callers (`gui_api`@1245, `gui_worker`@1608/1628, `gui_main`@122) behavior-compatible. **(B) Dependency integrity / reproducible build (R1-R10/RR1-C2/R1-R11):** new **`requirements-build.lock.txt`** (hash-pinned full tree via `pip-compile --generate-hashes --allow-unsafe` from the proven freeze, Windows/3.11, documented header+regen recipe); **`requirements.txt`** +explicit `cryptography==48.0.1` pin (EXISTING pdfminer.six transitive — lock integrity, NOT new dep) + platform/parity docs; new **`build/check_build_env.py`** asserts version.py↔requirements↔lock Playwright parity + cryptography pinned + lock hash-pinned & complete, and `--verify-installed` (self-fetches `pip freeze` — no fragile PowerShell pipe, BOM-tolerant) fails on unexpected/missing/drifted; **`build/build.ps1`** installs the lock with `--require-hashes`, static integrity gate before + exact-match verify after (fail on unexpected), new `-RecreateVenv`. **(C) `release.yml`** +guard asserting all 3 variant zips + each `.sha256` exist (fails-not-skips; R1-T06/M02; signing fully deferred RM06 — the SignPath integration is an inert deferred template, NOT implemented per-variant parity, unchanged); **`checks.yml`** wired `check_build_env.py` (the updater step already runs `check_updater.py`). **(D) Optional perf (A01) DROPPED** — harness built + measured (`import reports` ~937 ms over a 66 ms bare interp, on the startup path), but deferred: the registry is needed at bridge-init, `playwright` (~250 ms) loads via `common` regardless, and the safe realization is a report-registry refactor whose frozen-bundle correctness isn't offline-provable — recorded with data for roadmap (P11). **Verification:** 9 §J items **RED-proven** (new tests fail vs unmodified `updater.py`) → **GREEN**; fixed the one existing test that staged without a checksum. Lock proven installable `--require-hashes` 3× (fresh venvs, `pip check` clean, imports load) + **build.ps1 step-1 PowerShell replay** incl. a **negative pollution test** (extra `six` → verify FAILS exit 1). **FULL local frozen build** `build.ps1 -SelfTest` end-to-end: integrity gate PASS → lock install → `installed env matches the lock exactly` → PyInstaller (`Pruned 18 item(s)`) → exact-exe self-test `SMOKE OK` (chromium PDF 34772 B, pdfplumber, openpyxl, F6 trio, bridge 7 reports/252 routes, WebView+JS) → **`Frozen self-test PASSED`** (149 MB). `build.ps1` AST-parses; all 3 workflow YAMLs valid; the `release.yml` guard PS parses. Full suite **69/69** Python (68+`check_build_env`) + **3** Node + byte-compile + import smoke + `check_no_misspelling` + `check_import_direction` (no cycle) + `git diff --check` clean. Diff: 6 modified (`scripts/updater.py`, `build/check_updater.py`, `requirements.txt`, `build/build.ps1`, `release.yml`, `checks.yml`) + 2 new (`requirements-build.lock.txt`, `build/check_build_env.py`). **Owed (work-PC; §K2/§M; v0.18.1):** a real v0.17→v0.18 update on a managed PC (download/stage/re-hash/swap + the pagination/retry/checksum/death-check paths against live GitHub); the `.bat` source path's `--require-hashes` adoption. Planning folder not staged; not committed; not pushed. **Codex round 1 = `BLOCKED`** (1 blocking **P10-B01** — staged re-verify fail-open + exe-only; 1 rec **P10-A01** — SignPath wording). **Remediated (round 1):** **P10-B01 FIXED** — mandatory whole-bundle (`_EXE_NAME`+`_internal/**`+readmes) `_bundle_digest`, staging fails closed if it can't be recorded (`UPDATE_DIR` removed), apply fails closed on missing/malformed/mismatch before launch; behavioral probe reproduced both bypasses on the reconstructed old gate (`internal_tamper_allowed`/`missing_sidecar_allowed=True`) and proved the new code refuses both at **0 launches**; `check_updater.py` +`test_staged_bundle_reverify` +`test_staged_record_mandatory`. **P10-A01 FIXED (wording)** — signing documented fully deferred (RM06), inert template, no parity claim. Re-verified **69/69** Python + **3** Node green. **Codex round 2 = `PASS WITH FIXES`** (P10-B01 + P10-A01 Resolved; 1 required **P10-R01** — this coordination row still described the old fail-open path). **P10-R01 FIXED (this row, docs-only):** item (3) above now states the mandatory whole-bundle fail-closed behavior (no product-code change — Codex: no re-review needed for the wording fix). **Codex round 3 = `PASS WITH FIXES`** (B01/A01/R01 Resolved; 1 required **P10-R02** — the round-2 report line wrote the guarded product-name transposition verbatim, falsifying its own `check_no_misspelling` claim). **P10-R02 FIXED (round 3, report-only):** reworded that line (no literal misspelling) and re-ran `check_no_misspelling` AFTER appending the section → PASS (correcting the check-then-append ordering); no product-code change. **Codex round 4 = `PASS`** (no blocking/required/recs; B01/A01/R01/R02 all Resolved). **Committed `a8d9235`** "fix: harden the self-updater and hash-pin the reproducible build (P10)" — the **8** intended product files staged by explicit path (2 new: `build/check_build_env.py` + `requirements-build.lock.txt`; 6 modified: `scripts/updater.py` + `build/check_updater.py` + `requirements.txt` + `build/build.ps1` + `.github/workflows/release.yml` + `.github/workflows/checks.yml`); `docs/planning/` NOT staged; **not pushed**. Final verification at commit: Codex-cited targeted set + **68/68** other Python + **3** Node + byte-compile + `git diff --check` clean; a `git grep` for the transposed product-name spelling over all TRACKED files is **clean** (the only local `check_no_misspelling` hit is the transposed spelling inside Codex's UNTRACKED review file, never committed → CI/committed tree passes the guard); commit contains exactly the 8 product files (no `docs/planning`). **Owed (work-PC; §K2/§M; v0.18.1):** a real v0.17→v0.18 update on a managed PC exercising the live download→stage→bundle-re-verify→swap path. **Next eligible phase: P12** (`pending`; residual offline audit hardening + evidence harnesses; depends committed P2 + P3) — then P13/P11. |
| **P12** | `awaiting_review` | `a8d9235` | [`phases/P12-claude-report.md`](phases/P12-claude-report.md) | Residual offline audit hardening + evidence harnesses (CR-001 §I; depends committed P2 `e47b700` + P3 `ca3c2af`). **Four items, each its own offline check; `compare_core` + the locked row-emit logic untouched; no live access.** **(1) reset junction/symlink guard** — new `scripts/safe_delete.py::scoped_rmtree` (reparse-point-safe: unlinks a junction/symlink at the root OR any descendant, never recurses through it); `gui_worker.ResetWorker` uses it (was `shutil.rmtree`), `onerror`/locked-file reporting preserved. **HONEST (RM04 applied beyond PDFs):** on the shipped CPython 3.11 `shutil.rmtree` ALREADY refuses to follow junctions (a CHILD junction it unlinks; a ROOT reparse point it RAISES on + leaves), so the audit's data-destruction scenario does NOT reproduce on 3.11 — the guard's genuine value is a clean/UNIFORM unlink of a ROOT reparse point (reset hands every target to deletion as a root) instead of the stdlib's confusing error + lingering link, plus explicit version-independence; `check_reset_safety.py` asserts the TRUE 3.11 behavior (no false RED) on a real `mklink /J` junction. **(2) consolidate-overwrite TOCTOU re-check** — new `artifact_store.confirm_late_overwrite` + `atomic_save_if` (two-phase: serialize to temp, then gate `os.replace`); wired into ALL 7 confirm→write paths (`commit_workbook` + 6 consolidators, incl. the 2 per-route converters whose REAL re-check lives in the converter before the no-op-confirm inner `consolidate_xlsx`). Narrows (does NOT atomically eliminate) the confirm-then-appears window; the truncation half is already closed (F9 temp+`os.replace`). Default-confirm behavior is byte-identical; `compare_core` untouched. `check_consolidate_toctou.py` (helper unit + commit_workbook + consolidate_xlsx + ramp_summary + converter shape). **(3) M03 destination-ownership marker** — new `scripts/owned_dir.py` (`.tsmis-owned.json`); stamped on the Export-Everything store `<src>-<env>` folder (once, survives the per-report stage→swap) + the `comparisons` tree (both matrix workers, `if self.dest` guarded); `reset_targets` PREFERS the marker (proves app-creation, any name) with the legacy known-NAME trust kept as an explicit backward-compat fallback (no existing-install reset regression). Marker proven NON-inert — a foreign-named MARKED dir is reset-deletable; an unmarked foreign dir is preserved. `check_owned_dir.py` (round-trip + reject foreign/corrupt + a real `reset_targets` integration). **(4) PDF expected-row oracle + evidence-capture contract** — new `scripts/pdf_row_oracle.py`: counts expected Highway Log (PDF) rows by an INDEPENDENT method (text lines beginning with a postmile, recognizer authored separately from the parser — R1-D05/D12) vs the parser's cell-rect geometry; `reconcile()` flags any drop/dup; `capture_evidence()` is the PRIVACY-SAFE (counts only — never cell contents; RM05) record the P13 work-PC kit runs over REAL PDFs. `check_pdf_row_oracle.py` + `check_tsmis_pdf_reconcile` extended (oracle ↔ `parse_pdf` 3-tuple). **RM04 — does NOT over-claim:** synthetic TEXT-LINE fixtures prove the ORACLE LOGIC + capture WIRING only; the parser's real-PDF correctness (row-count / stale-geometry emit / ramp-summary misattribution) stays **v0.18.1 evidence-driven**. **Packaging/CI:** `build/app.spec` APP_MODULES +`safe_delete`/`owned_dir`/`pdf_row_oracle` (F6); `checks.yml` +the 4 new checks in cohesive blocking steps. **Verification:** full suite **72/72** Python (68 prior + 4 new) + **3** Node + `compileall scripts build version.py`; all touched-module regressions green (`check_artifact_store`/`p2_freshness`/`consolidate_outcome`/`consolidate_intersection`/`ramp_summary_partial`/`worker_lifecycle`/`matrix_tsn`/`compare_env_highway_log_pdf`/`highway_log_*`/`app_modules`/`import_direction`); `compare_core` comparator checks byte-identical; `git diff --check` clean; `git grep` for the transposed product name over all TRACKED files clean (the lone `check_no_misspelling` hit is the UNTRACKED Codex review file). Diff vs `a8d9235`: 11 modified (+224/−38) + 7 new (3 `scripts/` console-free modules + 4 `build/` checks). **Owed (work-PC; §K2/§M; v0.18.1):** live "Delete all reports" over a real store; real-PDF oracle reconciliation via the capture path; M03 on a real shared store. Planning folder not staged; not committed; not pushed. **Codex round 1 = `BLOCKED`** (1 blocking **P12-B01** — the consolidate re-check sat BEFORE the writer, so a destination appearing DURING the build/combine was still overwritten; 1 required **P12-R01** — the oracle missed the split realignment-prefix row shape `"R 012.345"` the parser accepts). **Remediated (round 1):** **P12-B01 FIXED** — the overwrite gate moved to the FINAL `os.replace` for every writer: the 3 direct writers (`ramp_summary`/`intersection_summary`/`tsn_highway_sequence`) route their `build_workbook`/`_write_workbook` save through `atomic_save_if(proceed=…)` and `consolidate()` returns `cancelled` on a declined commit; the 2 per-route converters pass the REAL confirm + `existed_at_confirm` into `consolidate_xlsx` (new `existed_at_confirm` param skips the converter's already-done initial prompt while keeping the pre-replace gate — no double prompt), unused `import artifact_store` removed from both. `check_consolidate_toctou.py` rewritten to make the destination appear AT the final save (patching `atomic_save_if`) — RED on the old pre-build re-check, GREEN now — covering all 3 direct writers + the converter + an `atomic_save_if` gate unit test. **P12-R01 FIXED** — `pdf_row_oracle.line_is_data_row` gained an independently-authored split-prefix recognizer (lone letter + bare postmile, not imported from the parser); `check_pdf_row_oracle.py` + split-prefix oracle/count/capture coverage; RED probe: old recognizer `False` for `"R 012.345"`, new `True`. **Follow-through:** `check_tsn_outcome.py` `_write_workbook` stub updated for the new `proceed` kwarg (no product change). Re-verified full suite **72/72** Python + **3** Node + byte-compile; touched-module regressions green; `compare_core` byte-identical; tracked-tree transposition scan clean. Diff now 12 modified + 7 new. Report: [`phases/P12-claude-report.md`](phases/P12-claude-report.md) remediation section. **Codex round 2 = `PASS WITH FIXES`** (B01/R01 resolved; 1 required **P12-R02** — `scripts/safe_delete.py`'s module docstring still over-claimed that `shutil.rmtree` recurses THROUGH junctions, the one spot the pre-investigation framing survived). **Remediated (round 2, docs-only):** **P12-R02 FIXED** — rewrote the `safe_delete.py` docstring to the verified CPython 3.11 behavior (child junction targets already preserved by `shutil.rmtree`; a ROOT reparse point refused + left; `scoped_rmtree` = explicit/uniform/version-independent root-or-descendant unlink), **no code/behavior change** (`scoped_rmtree`/`is_reparse_point`/ResetWorker byte-identical). Re-ran exactly what Codex asked: `check_reset_safety.py` PASS + `git diff --check` clean; byte-compile + transposition scan clean. Round-1 full-suite **72/72** + 3 Node stands. **Codex round 3 = `PASS`** (no blocking/required/recs; B01/R01/R02 all Resolved). **Committed `d15216d`** "fix: harden reset/consolidate file safety + add the PDF row oracle (P12)" — the **19** intended product/check files staged by explicit path (7 new: `scripts/safe_delete.py` + `scripts/owned_dir.py` + `scripts/pdf_row_oracle.py` + `build/check_reset_safety.py` + `build/check_consolidate_toctou.py` + `build/check_owned_dir.py` + `build/check_pdf_row_oracle.py`; 12 modified: `scripts/artifact_store.py` + `scripts/gui_worker.py` + the 6 consolidators + `build/app.spec` + `.github/workflows/checks.yml` + `build/check_tsmis_pdf_reconcile.py` + `build/check_tsn_outcome.py`); `docs/planning/` NOT staged; **not pushed**. Final verification at commit: full suite **72/72** Python + **3** Node + byte-compile; staged-index + committed-tree `git grep` for the transposed product name **clean** (the only local `check_no_misspelling` hit is the UNTRACKED Codex review file, absent from the commit → CI/committed tree passes); commit holds exactly the 19 product files (0 `docs/planning`). **Owed (work-PC; §K2/§M; v0.18.1):** live "Delete all reports" over a real store; real-PDF oracle reconciliation via `capture_evidence`; M03 on a real shared store. **Next eligible phase: P13** (`pending`; work-PC validation handoff + v0.18.1 close-out; depends committed P8c/P10/P12) — then P11(last). |

## Reminders

- **Agents must not write product code simultaneously.** Claude is the sole implementer; Codex is
  read-only review. One writer touches source at a time.
- **These planning files are NOT canonical product documentation.** Canonical knowledge lives in `docs/`
  (routed from `CLAUDE.md`); planning outcomes are folded into the canonical docs in P11, then the
  planning folder may be retired.
- **Do not stage, commit, or push the `docs/planning/` folder** (or any planning artifact) unless the user
  later explicitly requests it.
- No pushing, tagging, publishing, releasing, or AI attribution at any point.
