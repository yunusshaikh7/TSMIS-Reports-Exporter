# FINDINGS — sol-001 (Sol owns this file)

Out-of-scope or deferred discoveries. Do NOT recursively pursue categories 2–4 — document
and continue. Claude triages these into future missions.

**Classification:** (1) required & in scope — fix now · (2) blocking but out of scope ·
(3) non-blocking follow-up · (4) suspected but not confirmed.

| ID | Category | Severity | Area / file | Description + evidence | User impact | Blocks mission? | Confirmed? | Recommended follow-up |
|----|----------|----------|-------------|------------------------|-------------|-----------------|------------|-----------------------|
| F-01 | 2 | P2 | `build/check_source_zip_smoke.py:_candidate_archive` | Every authoritative run from this linked worktree fails because the check sets `GIT_ALTERNATE_OBJECT_DIRECTORIES=ROOT/.git/objects`, while `.git` is a worktree pointer file (`gitdir: .../.git/worktrees/TSMIS-sol-reliability`); `git read-tree HEAD` exits 128 with `unable to normalize alternate object path`. Final gate was 133/134 with only this red. An exact `git archive HEAD` with the release prefix passed the same check's supplied-archive membership and clean-extract smoke completely. | The pre-commit candidate builder cannot run directly from a linked worktree; the committed source ZIP and product runtime are unaffected. | exact default full-gate green only | yes | Shared build check should resolve the common object dir with `git rev-parse --git-common-dir` (or equivalent) instead of assuming `.git` is a directory. |
| F-02 | 1 | P2 | `scripts/updater.py:apply_update_and_restart` | The fixed two-second helper death poll returned success for a fake helper that stayed alive for five polls and died later. A one-use readiness nonce now requires the helper to prove it opened the original PID handle before the old app may close; the strengthened check was red, then green. | A slow-starting helper can no longer die outside an arbitrary window after the old app commits to closing. | no (fixed) | yes | Integrator should re-run the frozen/work-PC update path because endpoint policy timing is not reproducible offline. |
| F-03 | 1 | P2 | `scripts/updater.py:perform_swap` | A forced phase-2 install failure plus forced rollback failure proved `perform_swap` still called `_relaunch` on a mixed tree. Relaunch is now conditional on a coherent new tree or a fully restored old tree; partial rollback logs that relaunch was skipped and keeps the truthful reinstall dialog. | Prevents knowingly starting an executable against partially restored internals. | no (fixed) | yes | Re-run on the work PC with Defender/file-lock conditions when practical. |
| F-04 | 1 | P2 | `scripts/updater.py:download_and_stage` | Charter recheck found checksum verification already fail-closed even when asset size is absent; `test_checksum_required` proves unverified bytes are deleted and never staged. | No size-only/unverified install path remains. | no (proved existing fix) | yes | None. |
| F-05 | 1 | P3 | `scripts/updater.py:_rotate_swap_log` | Charter recheck found the helper log already rotates at 256 KiB and keeps one backup; `test_swap_log_rotates` proves the bound. | Long-lived portable installs do not accumulate an unbounded swap log. | no (proved existing fix) | yes | None. |
| F-06 | 1 | P3 | `scripts/updater.py:download_and_stage` | Charter recheck found a 60-second per-read socket timeout plus three bounded clean download attempts; `test_download_retries_transient` proves two transient failures recover on the third attempt. | Transient proxy/socket failures are retried without an infinite hang or unbounded loop. | no (proved existing fix) | yes | Work-PC corporate-proxy behavior remains a live verification item. |
| F-07 | 1 | P2 | `scripts/updater.py:_wait_for_helper_ready` | Regression review proved a nonce-only implementation would reject Settings > Revert when the staged older executable predates the protocol. New helpers now publish `starting:` before the existing helper-log line and must reach nonce-backed `ready:`; only a helper with no protocol marker may prove the legacy PID-wait line appended after the captured log cursor. The new Revert check failed before this negotiation and is green after it. | Keeps downgrade/revert functional without letting a nonce-capable helper silently fall back to the weaker legacy signal. | no (fixed) | yes | Exercise one real frozen Revert after integration; the offline fake proves protocol selection and stale-log exclusion, not process policy. |
| F-08 | 1 | P2 | `scripts/exporter.py:_retry_failed_combined` | The combined slow-retry pass deleted all first-pass failure rows, then failed to restore routes skipped by cancel or an unrecoverable recovery stop. A red check showed both edition results collapse to only the one successful retry; reconciliation now restores every unrecorded route once via the shared tally path and catches in-route `RunCancelled`. | Failed routes no longer disappear from final counts or both edition run-report CSVs. | no (fixed) | yes | None beyond normal work-PC combined-export retry acceptance. |
| F-09 | 1 | P2 | `scripts/batch_manifest.py:load` | `load()` validated only that `steps` was a list; entries such as a string or a dict missing `env` were accepted and then crashed `pending()` (`.get`/key access). Two red fixtures now fail closed during load via strict step-shape validation. | A corrupt/torn batch manifest degrades to “no resumable batch” instead of crashing the resume surface. | no (fixed) | yes | None. |
| F-10 | 4 | P3 | `build/check_pdf_role_provenance.py` | In one 133-check parallel gate run the off-limits check exited nonzero with no stdout/stderr. It immediately passed every assertion alone, then passed in the next full `-j 4` run. No owned-code dependency or reproducible failure was found. | None demonstrated; possible transient process/resource flake. | no | no | Monitor integration runs; investigate only if it recurs with output or a reproducible exit code. |
| F-11 | 1 | P3 | `scripts/browser_channels.py:_new_app_context` | The optional local-network permission retry swallowed its first launch exception completely. A red fake-browser check now proves the retry preserves storage state and logs exception type + first-line reason before dropping only the optional permission. | Older browser builds still receive the intended compatibility retry, and an uploaded log now explains why it happened. | no (fixed) | yes | None. |
| F-12 | 1 | P3 | `scripts/timeouts.py:_settings_ms`; `scripts/site_target.py:get_url` | Timeout fallback logged an unsanitized multiline exception, while site-URL fallback logged nothing. Red checks now prove both retain their compiled defaults and log type + first line only. | Settings corruption remains fail-safe without hiding the chosen fallback or producing ambiguous multiline log entries. | no (fixed) | yes | None. |
| F-13 | 1 | P3 | `scripts/report_nav.py:maybe_screenshot` | A failure while polling the GUI preview-request flag returned silently. The request remains best-effort/nonfatal, but a red check now requires worker number, exception type, and first-line reason. | A stuck Preview request can now be diagnosed from the same run log without disturbing exports. | no (fixed) | yes | None. |
| F-14 | 1 | P3 | `scripts/session.py`; `scripts/login.py` | Saved-session, headed-browser, and live Edge-capture fallbacks logged incomplete diagnostics (message without class or class without reason). Offline state-transition checks were red until every decision logged type + first-line reason while preserving the existing device/Chromium/profile fallback order. | Auth fallback behavior is unchanged, but managed-PC browser/policy failures are now actionable from one log. | no (fixed) | yes | Reconfirm headed console sign-in on a work PC when practical; console prompts are faked offline. |
| F-15 | 1 | P3 | `scripts/edge_device.py` | Both headed and silent persistent-Edge launches silently retried without the optional local-network permission. Fake persistent contexts proved retry arguments and return identity red-to-green; both paths now log the sanitized reason before retrying. | Compatibility with older Edge remains intact and permission-policy fallback is visible. | no (fixed) | yes | Managed-Edge policy behavior remains a work-PC acceptance item. |
| F-16 | 3 | P3 | `.github/workflows/checks.yml`; `build/check_ci_manifest.py` | The charter says to append new checks to the CI YAML, but the repository intentionally stopped enumerating checks in v0.19.0. CI runs `build/run_checks.py`, which auto-globs every non-audit `check_*.py`, and `check_ci_manifest.py` guards that exact contract. Editing YAML to enumerate these checks would contradict the current source of truth. | None; both new checks are already part of local and CI discovery by filename. | no | yes | Integrator should keep the auto-discovery contract; no YAML change is recommended. |

## Required-change findings (a fix needs a Claude-owned / shared-infra file)

- **F-01:** `build/check_source_zip_smoke.py` is outside this lane's owned checks. Its Git-object
  lookup must become worktree-aware for the charter's exact full-gate command to be green here.
  Reliability implementation and the other 131 checks remain independently runnable.

## Cannot-verify-offline

- Live TSMIS report generation, site DOM drift, OAuth/SAML completion, and true transient
  export-click behavior require the Caltrans intranet/work PC; this mission exercises only offline
  state transitions and fakes.
- Defender/indexer timing, managed-Edge policy behavior, the corporate proxy/TLS inspection path,
  and unsigned-exe swap/relaunch behavior require the physical locked-down work PC. Windows file
  mechanics can be exercised locally, but those endpoint controls cannot.
- GUI `#mock` behavior is outside this lane; no GUI files or checks will be changed.
